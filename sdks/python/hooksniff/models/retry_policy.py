# coding: utf-8

"""
    HookSniff API

    Webhook delivery, monitoring, and management API. All endpoints under `/v1` require authentication via `Authorization: Bearer <api_key>` header unless marked as **Public**. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@hooksniff.vercel.app
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RetryPolicy(BaseModel):
    """
    RetryPolicy
    """ # noqa: E501
    max_attempts: StrictInt
    backoff: StrictStr
    initial_delay_secs: StrictInt
    max_delay_secs: StrictInt
    __properties: ClassVar[List[str]] = ["max_attempts", "backoff", "initial_delay_secs", "max_delay_secs"]

    @field_validator('backoff')
    def backoff_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['exponential', 'linear', 'fixed']):
            raise ValueError("must be one of enum values ('exponential', 'linear', 'fixed')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RetryPolicy from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RetryPolicy from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "max_attempts": obj.get("max_attempts") if obj.get("max_attempts") is not None else 3,
            "backoff": obj.get("backoff") if obj.get("backoff") is not None else 'exponential',
            "initial_delay_secs": obj.get("initial_delay_secs") if obj.get("initial_delay_secs") is not None else 10,
            "max_delay_secs": obj.get("max_delay_secs") if obj.get("max_delay_secs") is not None else 3600
        })
        return _obj


