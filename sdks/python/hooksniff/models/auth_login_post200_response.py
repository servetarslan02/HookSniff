# coding: utf-8

"""
    HookSniff API

    Webhook delivery, monitoring, and management API. All endpoints under `/v1` require authentication via `Authorization: Bearer <api_key>` header unless marked as **Public**. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@hooksniff.vercel.app
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from hooksniff.models.auth_response import AuthResponse
from hooksniff.models.two_factor_required_response import TwoFactorRequiredResponse
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

AUTHLOGINPOST200RESPONSE_ONE_OF_SCHEMAS = ["AuthResponse", "TwoFactorRequiredResponse"]

class AuthLoginPost200Response(BaseModel):
    """
    AuthLoginPost200Response
    """
    # data type: AuthResponse
    oneof_schema_1_validator: Optional[AuthResponse] = None
    # data type: TwoFactorRequiredResponse
    oneof_schema_2_validator: Optional[TwoFactorRequiredResponse] = None
    actual_instance: Optional[Union[AuthResponse, TwoFactorRequiredResponse]] = None
    one_of_schemas: Set[str] = { "AuthResponse", "TwoFactorRequiredResponse" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = AuthLoginPost200Response.model_construct()
        error_messages = []
        match = 0
        # validate data type: AuthResponse
        if not isinstance(v, AuthResponse):
            error_messages.append(f"Error! Input type `{type(v)}` is not `AuthResponse`")
        else:
            match += 1
        # validate data type: TwoFactorRequiredResponse
        if not isinstance(v, TwoFactorRequiredResponse):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TwoFactorRequiredResponse`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in AuthLoginPost200Response with oneOf schemas: AuthResponse, TwoFactorRequiredResponse. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in AuthLoginPost200Response with oneOf schemas: AuthResponse, TwoFactorRequiredResponse. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into AuthResponse
        try:
            instance.actual_instance = AuthResponse.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into TwoFactorRequiredResponse
        try:
            instance.actual_instance = TwoFactorRequiredResponse.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into AuthLoginPost200Response with oneOf schemas: AuthResponse, TwoFactorRequiredResponse. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into AuthLoginPost200Response with oneOf schemas: AuthResponse, TwoFactorRequiredResponse. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], AuthResponse, TwoFactorRequiredResponse]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


