language: ruby
cache: bundler
rvm:
  - 2.7
  - 3.0
  - 3.1
script:
  - bundle install --path vendor/bundle
  - bundle exec rspec
  - gem build hooksniff-sdk.gemspec
  - gem install ./hooksniff-sdk-0.3.0.gem
