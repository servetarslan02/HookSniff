apiVersion: v1
kind: Service
metadata:
  name: hookrelay-dashboard
  namespace: hookrelay
  labels:
    app: hookrelay-dashboard
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: hookrelay-dashboard
