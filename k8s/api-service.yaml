apiVersion: v1
kind: Service
metadata:
  name: hookrelay-api
  namespace: hookrelay
  labels:
    app: hookrelay-api
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: hookrelay-api
