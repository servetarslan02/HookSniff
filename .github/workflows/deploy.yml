name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}

jobs:
  # Only deploy if CI succeeded
  build-and-push:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: [api, worker, dashboard]
        include:
          - service: api
            dockerfile: Dockerfile.api
            image: hooksniff-api
          - service: worker
            dockerfile: Dockerfile.worker
            image: hooksniff-worker
          - service: dashboard
            dockerfile: Dockerfile.dashboard
            image: hooksniff-dashboard
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_PREFIX }}/${{ matrix.image }}
          tags: |
            type=sha
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ──────────────────────────────────────────────
  # Deploy step — configure for your infrastructure
  # ──────────────────────────────────────────────
  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    # Uncomment and configure for your deployment target:
    #
    # Example: Kubernetes via kubectl
    # steps:
    #   - uses: actions/checkout@v4
    #   - name: Set up kubectl
    #     uses: azure/setup-kubectl@v3
    #   - name: Configure kubeconfig
    #     run: echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config
    #   - name: Deploy to cluster
    #     run: |
    #       kubectl set image deployment/hooksniff-api \
    #         api=${{ env.IMAGE_PREFIX }}/hooksniff-api:sha-${GITHUB_SHA::7} \
    #         -n hooksniff
    #       kubectl set image deployment/hooksniff-worker \
    #         worker=${{ env.IMAGE_PREFIX }}/hooksniff-worker:sha-${GITHUB_SHA::7} \
    #         -n hooksniff
    #       kubectl rollout status deployment/hooksniff-api -n hooksniff
    #       kubectl rollout status deployment/hooksniff-worker -n hooksniff
    #
    # Example: Docker Compose on VPS
    # steps:
    #   - name: Deploy via SSH
    #     uses: appleboy/ssh-action@v1
    #     with:
    #       host: ${{ secrets.DEPLOY_HOST }}
    #       username: ${{ secrets.DEPLOY_USER }}
    #       key: ${{ secrets.DEPLOY_KEY }}
    #       script: |
    #         cd /opt/hooksniff
    #         docker compose -f docker-compose.prod.yml pull
    #         docker compose -f docker-compose.prod.yml up -d
    steps:
      - name: Placeholder — configure your deployment
        run: |
          echo "⚠️  Configure your deployment target in this workflow."
          echo "See comments above for Kubernetes and Docker Compose examples."
          echo "Images pushed: ghcr.io/${{ github.repository_owner }}/hooksniff-{api,worker,dashboard}"
