{
  "name": "hooksniff",
  "private": true,
  "scripts": {
    "vercel-build": "cd dashboard && npm ci && npm run build",
    "render-deploy": "bash deploy.sh"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.21.0"
  }
}
