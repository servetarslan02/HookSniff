# 2026-05-17 — gcloud Build Fix

## Oturum — 00:45 GMT+8

### Yapılan İşler

**gcloud Build Hatası Düzeltildi**
- Hata: `E0004: non-exhaustive patterns` — `api/src/ws/bridge.rs:51`
- `AppEvent` enum'unda 9 variant vardı ama `convert_to_ws_event()` sadece 5'ini handle ediyordu
- Eklenen match arm'ları:
  1. `EndpointCreated` → `endpoint.created`
  2. `EndpointUpdated` → `endpoint.updated`
  3. `EndpointDeleted` → `endpoint.deleted`
  4. `AlertTriggered` → `alert.triggered`

**Warning Düzeltmeleri**
- `admin.rs:3148` — Gereksiz `mut` kaldırıldı (`query_builder_count`, `query_builder_data`)
- `admin.rs:3403,4039` — Kullanılmayan `status` değişkeni `_status` olarak değiştirildi

**Commit:** `86c28806` — main branch, push başarılı ✅

### Değişen Dosyalar
1. `api/src/ws/bridge.rs` — 4 yeni match arm eklendi (+48 satır)
2. `api/src/routes/admin.rs` — 3 warning düzeltmesi
