# 2026-05-21 — Session 5 Log (22:55-23:15 GMT+8)

## Yapılan İşler

### 1. Team Role-Based Access Control (RBAC) — Tam Uygulama (22:55-23:12)

**Sorun:**
- `require_team_developer` ve `require_team_analyst` tanımlı ama hiçbir endpoint'te kullanılmıyordu
- Endpoint, API key, webhook, application gibi kaynaklarda hiç rol kontrolü yoktu
- Viewer bile endpoint oluşturabiliyordu
- SSO `default_role` atıyordu ama bu rol hiçbir yerde enforce edilmiyordu

**Çözüm:**
- `teams.rs`'te `require_role`, `require_team_member`, `require_team_admin`, `require_team_developer`, `require_team_analyst` fonksiyonları `pub` yapıldı
- 18 route dosyasına rol kontrolü eklendi:

| Router | Write (create/update) | Destructive (delete/rotate) |
|--------|----------------------|---------------------------|
| endpoints | developer | admin |
| api_keys | developer | admin |
| applications | developer | admin |
| webhooks | developer | — |
| transforms | developer | admin |
| alerts | developer | admin |
| routing | developer | — |
| schemas | developer | — |
| templates | developer | — |
| integrations | developer | admin |
| connectors | developer | admin |
| environments | developer | admin |
| environments(vars) | developer | admin |
| rate_limits | developer | admin |
| operational_webhooks | developer | admin |
| background_tasks | — | admin (cancel) |
| service_tokens | admin (zaten owner check var) | — |
| custom_domains | developer | admin |

**Rol Hiyerarşisi:**
- owner > admin(40) > developer(30) > analyst(20) > viewer(10)
- Write/mutate ops: require_team_developer (admin+developer yapabilir)
- Read/list ops: rol değişikliği yok (tüm üyeler okuyabilir)
- Destructive ops: require_team_admin (sadece admin+owner)

**Commit:** `5b22428e` — "fix: enforce team role-based access control on all resource endpoints"

### 2. RBAC Refactor — check_user_team_role (23:15-23:23)

**Sorun:** Inline pattern sadece 1 rastgele takım kontrol ediyordu (LIMIT 1).
Kullanıcı team A'da viewer, team B'de admin ise engelleniyordu.

**Çözüm:**
- `check_user_team_role()` fonksiyonu `teams.rs`'e eklendi — TÜM takım üyeliklerini kontrol eder
- Kullanıcı herhangi bir takımda gerekli role sahipse geçer
- Team owner'ı her zaman tam erişime sahip
- 36 inline `team_members` sorgusu merkezi fonksiyonla değiştirildi
- Net -126 satır (94 eklendi, 220 silindi)

**Commit:** `a9929b3a` — "refactor: centralize RBAC with check_user_team_role — checks ALL teams"

### 3. SSO default_role Fix (23:26-23:30)

**Sorun:** SAML ve OIDC callback'lerinde auto-join sırasında rol **hardcoded "viewer"** olarak atanıyordu. SSO config'de `default_role: "developer"` ayarlansın bile, kullanıcı hep viewer olarak ekleniyordu.

**Çözüm:**
- `SsoLoginState` struct'ına `default_role` alanı eklendi
- 3 SSO config lookup query'sine `default_role` eklendi
- SAML callback: `login_state.default_role` kullanıyor
- OIDC callback: `login_state.default_role` kullanıyor
- `auto_join_default_team` (legacy path) zaten config'den okuyordu

**Commit:** `b39966c8` — "fix: SSO now uses default_role from config instead of hardcoded 'viewer'"

### 2. Değişen Dosyalar (18 dosya, +532 satır)
- `api/src/routes/teams.rs` — pub visibility for role functions
- `api/src/routes/endpoints.rs` — find_primary_team helper + role checks on all write ops
- `api/src/routes/api_keys.rs` — role checks on create/delete/rotate
- `api/src/routes/applications.rs` — role checks on create/update/delete
- `api/src/routes/webhooks.rs` — role checks on create/batch/replay/batch_replay
- `api/src/routes/transforms.rs` — role checks on create/update/delete
- `api/src/routes/alerts.rs` — role checks on create/update/delete
- `api/src/routes/routing.rs` — role check on update_routing
- `api/src/routes/schemas.rs` — role check on register_schema
- `api/src/routes/templates.rs` — role check on apply_template + Uuid import
- `api/src/routes/integrations.rs` — role checks on create/update/delete
- `api/src/routes/connectors.rs` — role checks on create/update/delete
- `api/src/routes/environments.rs` — role checks on env + variable CRUD
- `api/src/routes/rate_limits.rs` — role checks on set/delete
- `api/src/routes/operational_webhooks.rs` — role checks on create/update/delete
- `api/src/routes/background_tasks.rs` — role check on cancel
- `api/src/routes/service_tokens.rs` — admin check on create
- `api/src/routes/custom_domains.rs` — role checks on add/delete

## Sonraki Adımlar
- Blog içerikleri yaz (SEO)
- Sitemap'e blog sayfalarını ekle
- Dashboard billing sayfası tutarsızlığını düzelt
- Search Console performansını kontrol et
