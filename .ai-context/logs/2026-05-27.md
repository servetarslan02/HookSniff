# Oturum Log — 2026-05-27

## Kullanıcı
- **İsim:** Servet Arslan
- **Dil:** Türkçe
- **İlk oturum:** Evet (OpenClaw kurulumu)

## Yapılan İşler

### 1. OpenClaw Kurulumu
- GitHub repo klonlandı (HookSniff)
- `.ai-context/` hafıza sistemi okundu
- Kimlik oluşturuldu (Hook 🪝)
- USER.md, SOUL.md, IDENTITY.md güncellendi
- BOOTSTRAP.md silindi

### 2. Dashboard Hata Düzeltmeleri
Servet "müşteri panelinde hata var girdiğimde tekrar deneyin diyor" dedi. Tarayıcıda kontrol edildi:
- `/core` → "Something went wrong" hatası
- `/applications` → Aynı hata
- `/deliveries` → Aynı hata
- Tüm kullanıcı panel sayfaları çökük

#### Tespit Edilen ve Düzeltilen Hatalar:

**Hata 1: `QueryClientRequiredError`**
- `@tanstack/query-db-collection` paketi `queryClient` istiyor ama verilmemiş
- Düzeltme: `collections.ts` ve `useCollections.ts`'e `queryClient` parametresi eklendi

**Hata 2: `QueryBuilderError` (plain array)**
- `useLiveQuery` callback'inde collection null iken `[]` array gönderilmiş
- TanStack DB plain array kabul etmiyor
- Düzeltme: Callback'ten `null` döndürüldü

**Hata 3: `useDeliveryLogs` import eksik**
- `DeliveriesContent.tsx`'te `useDeliveryLogs` kullanılmış ama import edilmemiş
- Düzeltme: Import satırına eklendi

**Hata 4: `applications.noApplications` i18n eksik**
- `en.json` ve `tr.json`'da `applications` namespace'inde `noApplications` anahtarı yok
- Düzeltme: Her iki dile de eklendi

**Hata 5: Undefined data crash**
- TanStack DB live query'lerden dönen verilerde undefined item'lar var
- `getAppLabels(app)` → `app.is_active` → TypeError
- `endpointUrlMap` → `ep.id` → TypeError
- Düzeltme: `safeMap` helper ile tüm hook'larda null guard

### 3. Vercel Deploy Sorunları
- Git email `ai@hooksniff.dev` → Vercel blokladı
- `servetarslan02@gmail.com` olarak düzeltildi
- CLI yanlış projeye (`dashboard`) deploy ediyordu
- `hooksniff-dash` projesine link yapıldı
- `hooksniff.vercel.app`'a başarıyla deploy edildi

### 4. Kullanıcı Bilgileri
- **Email:** servetarslan02@gmail.com
- **Vercel token:** Mevcut (CLI ile deploy)
- **GitHub token:** Mevcut (repo erişimi)
- **Teknik bilgi:** Yok — kodu AI yazıyor

## Commit'ler
| Hash | Açıklama |
|------|----------|
| `1cdd067a` | fix: queryClient added to collections |
| `bbb0f5cf` | fix: useLiveQuery null guard |
| `2b604fd7` | fix: i18n noApplications + null guard |
| `2b233d56` | fix: useDeliveryLogs import |
| `d56c1ada` | fix: safeMap null guard on all hooks |

## Öğrenilen Dersler
- Vercel CLI `--name` flag'i proje seçimi için çalışmıyor → `vercel link` kullanılmalı
- `hooksniff.vercel.app` → `hooksniff-dash` projesine bağlı (farklı Vercel hesabı)
- TanStack DB `useLiveQuery` plain array kabul etmiyor → null döndürülmeli
- TanStack DB live query'lerinde undefined item olabilir → her zaman guard ekle
- Kullanıcı teknik bilgiye sahip değil → basit Türkçe açıklamalar yap
