# React Query Migration — Batch 3

**Date:** 2026-05-16

## Pages Converted

### 1. `dashboard/src/app/[locale]/(dashboard)/transforms/page.tsx`

**API calls identified:**
- `endpointsApi.list(token)` — fetches endpoints list
- `transformsApi.list(token, endpointId)` — fetches transform rules for selected endpoint
- `transformsApi.create(token, endpointId, data)` — creates a transform rule
- `transformsApi.delete(token, endpointId, ruleId)` — deletes a transform rule

**Hooks used:**
- `useEndpoints()` — already existed in useDashboardData.ts
- `useTransformRules(endpointId)` — **new** — fetches rules with Zod validation
- `useCreateTransformRule()` — **new** — mutation with auto-invalidation
- `useDeleteTransformRule()` — **new** — mutation with auto-invalidation

**Changes:**
- Removed `useState` for `endpoints`, `rules`, `loading`, `_error`
- Removed `useEffect` for endpoint list and rule loading
- Removed `useCallback` for `loadRules`
- Removed `useAuth` import (hooks handle auth internally)
- Removed `tc` (common translations) — no longer needed for manual error messages
- Kept all form state (`filterInclude`, `filterExclude`, `mapSource`, `mapTarget`, `enrichKey`, `enrichValue`)
- Kept `showCreate`, `deleteTarget` UI state
- Kept `handleCreate`, `handleDelete`, `confirmDelete` as imperative functions using mutations
- `handleCreate` now uses `createMutation.mutateAsync()` instead of manual API call + `loadRules()`
- `confirmDelete` now uses `deleteMutation.mutateAsync()` instead of manual API call + `setRules(prev => ...)`

---

### 2. `dashboard/src/app/[locale]/(dashboard)/sso/page.tsx`

**API calls identified:**
- `apiFetch('/sso/config', { token })` — fetches SSO configuration
- `apiFetch('/sso/config', { method: 'POST', body, token })` — saves SSO configuration (mutation, kept as-is)
- `ssoApi.testSso(token)` — tests SSO connection (mutation, kept as-is)

**Hooks used:**
- `useSsoConfig()` — **new** — fetches SSO config with Zod validation

**Changes:**
- Removed `useCallback` for `fetchConfig`
- Removed `useEffect` for config loading
- Added `useEffect` to sync `ssoConfig` data → form fields (one-way: data → form)
- Kept all form state (`provider`, `metadata`, `entityId`, `ssoUrl`, `certificate`, `enabled`)
- Kept `saving`, `testing` as local UI state
- Kept `handleSave` and `handleTest` as imperative functions (they're mutations with complex logic)
- Loading state now derived from `useSsoConfig().isLoading`

---

### 3. `dashboard/src/app/[locale]/(dashboard)/inbound/page.tsx`

**API calls identified:**
- `endpointsApi.list(token)` — fetches endpoints list
- `inboundApi.listConfigs(token)` — fetches inbound webhook configs
- `inboundApi.createConfig(token, data)` — creates an inbound config (mutation)

**Hooks used:**
- `useEndpoints()` — already existed in useDashboardData.ts
- `useInboundConfigs()` — **new** — fetches configs with Zod validation
- `useCreateInboundConfig()` — **new** — mutation with auto-invalidation

**Changes:**
- Removed `useEffect` with `Promise.all` for parallel data fetching
- Removed `useAuth` import (hooks handle auth internally)
- Removed `token` variable and guard check in `handleCreate`
- Kept all form state (`showCreate`, `selectedProvider`, `selectedEndpoint`, `webhookSecret`)
- `handleCreate` now uses `createMutation.mutateAsync()` — no manual re-fetch needed (auto-invalidation)

---

## New Hooks Added

### In `dashboard/src/hooks/useDashboardData.ts`:

| Hook | Type | Query Key | Description |
|------|------|-----------|-------------|
| `useTransformRules(endpointId)` | useQuery | `['transforms', endpointId]` | Fetches transform rules for an endpoint |
| `useCreateTransformRule()` | useMutation | — | Creates a transform rule, invalidates `['transforms', endpointId]` |
| `useDeleteTransformRule()` | useMutation | — | Deletes a transform rule, invalidates `['transforms', endpointId]` |
| `useInboundConfigs()` | useQuery | `['inbound-configs']` | Fetches inbound webhook configurations |
| `useCreateInboundConfig()` | useMutation | — | Creates inbound config, invalidates `['inbound-configs']` |
| `useSsoConfig()` | useQuery | `['sso-config']` | Fetches SSO/SAML/OIDC configuration |

## New Schemas Added

### In `dashboard/src/schemas/api.ts`:

| Schema | Type | Description |
|--------|------|-------------|
| `TransformRuleSchema` | Zod | Validates transform rule with filter/mappings/enrich |
| `InboundConfigSchema` | Zod | Validates inbound webhook config |
| `SsoConfigSchema` | Zod | Validates SSO configuration (SAML + OIDC fields) |

## Files Modified

1. `dashboard/src/schemas/api.ts` — added 3 schemas + types
2. `dashboard/src/hooks/useDashboardData.ts` — added 6 hooks + 3 imports
3. `dashboard/src/app/[locale]/(dashboard)/transforms/page.tsx` — rewritten
4. `dashboard/src/app/[locale]/(dashboard)/sso/page.tsx` — rewritten
5. `dashboard/src/app/[locale]/(dashboard)/inbound/page.tsx` — rewritten
