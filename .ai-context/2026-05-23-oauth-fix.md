# 2026-05-23 — OAuth Fix Session

## Yapılan İşler

### 1. OAuth URL Düzeltmesi (Frontend)
- `dashboard/src/app/[locale]/login/content.tsx`
- **Sorun:** OAuth butonları `/api/oauth/google` ve `/api/oauth/github` adresine gidiyordu
- Backend endpoint'leri `/v1/oauth/google` ve `/v1/oauth/github` altında
- Vercel rewrite `/api/:path*` → backend `/:path*` yapıyor → `/api/oauth/google` → `backend/oauth/google` (404!)
- **Çözüm:** Fallback URL `/api` → `/api/v1` olarak değiştirildi
- Artık: `/api/v1/oauth/google` → Vercel rewrite → `backend/v1/oauth/google` ✅
- Push: `8bcba4ee`

### 2. cloudbuild.yaml — OAuth Secrets Eklendi
- 4 API deploy bölgesine (europe-west1, europe-west3, me-west1, us-central1) eklendi:
  - `OAUTH_REDIRECT_BASE=https://hooksniff-api-1046140057667.europe-west1.run.app` (env var)
  - `GOOGLE_CLIENT_ID=google-client-id:latest` (secret)
  - `GOOGLE_CLIENT_SECRET=google-client-secret:latest` (secret)
  - `GITHUB_CLIENT_ID=github-client-id:latest` (secret)
  - `GITHUB_CLIENT_SECRET=github-client-secret:latest` (secret)

### 3. GCP Secret Manager — Secret'lar Oluşturuldu
- `google-client-id` → ⚠️ Placeholder (gerçek değer girilmeli)
- `google-client-secret` → ✅ Mevcut (GOCSPX-...)
- `github-client-id` → ⚠️ Placeholder (gerçek değer girilmeli)
- `github-client-secret` → ⚠️ Placeholder (gerçek değer girilmeli)

## Servet'in Yapması Gereken

### Adım 1: Google OAuth Client ID Bul
1. https://console.cloud.google.com/apis/credentials → proje: hooksniff-app
2. Mevcut OAuth 2.0 Client ID'yi bul (secret var, client oluşturulmuş olmalı)
3. Client ID'yi kopyala (xxx.apps.googleusercontent.com formatında)

### Adım 2: GitHub OAuth App Oluştur
1. https://github.com/settings/developers → New OAuth App
2. Application name: HookSniff
3. Homepage URL: https://hooksniff.vercel.app
4. Callback URL: https://hooksniff-api-1046140057667.europe-west1.run.app/v1/oauth/github/callback
5. Client ID ve Client Secret'i kopyala

### Adım 3: Secret Manager Güncelle
Servet değerleri verirse Python scripti ile güncellenebilir.

### Adım 4: Cloud Build Tetikle
Push veya manuel tetikleme → API deploy olacak.

## Commit
- `8bcba4ee` — fix(oauth): correct Google/GitHub OAuth button URLs and add OAuth secrets to Cloud Build
# Last OAuth config update: 2026-05-22T18:06:14Z

## Avatar Desteği Eklendi (02:08)

### Migration
- `086_avatar_url.sql` — `customers.avatar_url` TEXT sütunu

### Backend
- `Customer` struct → `avatar_url: Option<String>`
- `CustomerResponse` → `avatar_url: Option<String>`
- `CUSTOMER_SELECT` → `avatar_url` eklendi
- OAuth: Google userinfo API'den `picture` çekiliyor
- OAuth: GitHub user API'den `avatar_url` çekiliyor
- Yeni kayıt: avatar_url INSERT'te kaydediliyor
- Mevcut kullanıcı: avatar_url yoksa UPDATE ile ekleniyor

### Frontend
- `User` interface → `avatar_url?: string`
- Sidebar dropdown → OAuth profil resmi gösteriliyor (fallback: harf avatarı)
- `referrerPolicy="no-referrer"` — Google CDN'den resim çekerken sorun olmaması için

### Commit
- `0d036ba5` — feat(oauth): fetch and display profile pictures from Google/GitHub
