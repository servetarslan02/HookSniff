# React Query Migration — Batch 2

**Date:** 2026-05-16

## Pages Converted

### 1. `dashboard/src/app/[locale]/(dashboard)/billing/page.tsx`
- **API calls:** `billingApiExtended.getUsage()`, `billingApiExtended.getInvoices()`
- **Hooks used:** `useBillingUsage`, `useBillingInvoices` (new)
- **Changes:**
  - Removed `useState` for `usageCount`, `usageLimit`, `chartData`, `loadingUsage`, `invoices`, `loadingInvoices`
  - Removed 2 `useEffect` blocks for data fetching
  - Kept all modal/form UI state as `useState`
  - Derived `usageCount`, `usageLimit`, `chartData` from `useBillingUsage` data
  - Used `?? []` fallback for invoices in `InvoiceTable` props

### 2. `dashboard/src/app/[locale]/(dashboard)/applications/page.tsx`
- **API calls:** `applicationsApi.list()`
- **Hooks used:** `useApplications` (new)
- **Changes:**
  - Removed `useState` for `apps`, `loading`
  - Removed `useEffect` for fetching applications
  - Kept all UI state (create form, edit modal, search, delete confirmation)
  - Added toast notifications for create success

### 3. `dashboard/src/app/[locale]/(dashboard)/applications/[id]/page.tsx`
- **API calls:** `applicationsApi.get()`, `endpointsApi.list()`, `webhooksApi.list()`
- **Hooks used:** `useApplicationDetail(id)` (new, composite)
- **Changes:**
  - Removed `useState` for `app`, `endpoints`, `deliveries`, `loading`
  - Removed `useEffect` with `Promise.all` for fetching 3 APIs
  - Hook handles filtering endpoints/deliveries by application
  - Kept tab switching as `useState`

### 4. `dashboard/src/app/[locale]/(dashboard)/alerts/page.tsx`
- **API calls:** `alertsApi.list()`, `alertsApi.create()`, `alertsApi.update()`, `alertsApi.delete()`, `alertsApi.test()`
- **Hooks used:** `useAlerts`, `useCreateAlert`, `useUpdateAlert`, `useDeleteAlert`, `useTestAlert` (all new)
- **Changes:**
  - Removed `useState` for `alerts`, `loading`
  - Removed `useCallback` + `useEffect` for fetching alerts
  - Replaced manual `fetchAlerts()` calls with mutation `onSettled` auto-invalidation
  - Used `mutation.isPending` instead of manual `creating`/`saving` state
  - Kept all form/modal UI state as `useState`

### 5. `dashboard/src/app/[locale]/(dashboard)/team/page.tsx`
- **API calls:** `teamsApi.list()`, `teamsApi.listMembers()`, `teamsApi.create()`, `teamsApi.inviteMember()`, `teamsApi.removeMember()`, `teamsApi.updateRole()`, `teamsApi.acceptInvite()`
- **Hooks used:** `useTeams`, `useTeamMembers`, `useCreateTeam`, `useInviteTeamMember`, `useRemoveTeamMember`, `useUpdateTeamMemberRole`, `useAcceptTeamInvite` (all new)
- **Changes:**
  - Removed `useState` for `teams`, `selectedTeam`, `members`, `loading`
  - Removed `useCallback` + `useEffect` for fetching teams and members
  - Selected team derived from `teams.find()` using `selectedTeamId` state
  - Used `mutateAsync` for all mutations with try/catch error handling
  - Kept modal UI state as `useState`

## New Zod Schemas Added (`schemas/api.ts`)
- `ApplicationSchema` — validates application objects
- `InvoiceSchema` — validates invoice objects
- `BillingUsageSchema` — validates billing usage with nested webhooks/endpoints objects
- `TeamSchema` — validates team objects
- `TeamMemberSchema` — validates team member objects
- `AlertsListResponseSchema` — array of `AlertRuleSchema` (existing)

## New Hooks Added (`useDashboardData.ts`)
- `useBillingUsage()` — fetches billing usage with Zod validation
- `useBillingInvoices()` — fetches invoices with Zod validation
- `useApplications()` — fetches application list with Zod validation
- `useApplicationDetail(id)` — composite hook fetching app + endpoints + deliveries
- `useAlerts()` — fetches user alert rules
- `useCreateAlert()` — mutation with auto-invalidation
- `useUpdateAlert()` — mutation with auto-invalidation
- `useDeleteAlert()` — mutation with auto-invalidation
- `useTestAlert()` — fire-and-forget mutation
- `useTeams()` — fetches team list
- `useTeamMembers(teamId)` — fetches members for a specific team
- `useCreateTeam()` — mutation with auto-invalidation
- `useInviteTeamMember()` — mutation with auto-invalidation
- `useRemoveTeamMember()` — mutation with auto-invalidation
- `useUpdateTeamMemberRole()` — mutation with auto-invalidation
- `useAcceptTeamInvite()` — mutation with auto-invalidation

## Files Modified
1. `dashboard/src/schemas/api.ts` — added 6 new schemas
2. `dashboard/src/hooks/useDashboardData.ts` — added 16 new hooks
3. `dashboard/src/app/[locale]/(dashboard)/billing/page.tsx` — rewritten
4. `dashboard/src/app/[locale]/(dashboard)/applications/page.tsx` — rewritten
5. `dashboard/src/app/[locale]/(dashboard)/applications/[id]/page.tsx` — rewritten
6. `dashboard/src/app/[locale]/(dashboard)/alerts/page.tsx` — rewritten
7. `dashboard/src/app/[locale]/(dashboard)/team/page.tsx` — rewritten
