//! Webhook delivery workflows.
//!
//! NOTE: The Temporal-based workflows have been replaced by direct
//! PostgreSQL polling in main.rs. This module is kept as a placeholder
//! for future workflow implementations if needed.

// All workflow logic is now in main.rs (process_pending + retry loop).
// This module is intentionally empty.
