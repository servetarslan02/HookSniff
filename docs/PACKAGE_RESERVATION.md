# Paket Adı Rezervasyon Planı

## npm — @hooksniff/sdk

Scope: `@hooksniff`
Paket: `@hooksniff/sdk`

```bash
# npm'de scope oluştur
npm login
npm publish --access public
```

SDK'lar:
- `@hooksniff/sdk` (Node.js)
- `@hooksniff/cli` (CLI tool)

## PyPI — hooksniff

Paket: `hooksniff`

```bash
# PyPI'de paket adı
pip install twine
python setup.py sdist
twine upload dist/*
```

## crates.io — hooksniff

Paket: `hooksniff`

```bash
# crates.io'da publish
cargo publish
```

## Terraform Registry

Provider: `terraform-provider-hooksniff`

```bash
# Terraform Registry'de publish
# 1. GitHub repo: terraform-provider-hooksniff
# 2. GPG key ile imzala
# 3. Registry'ye submit
```

## Durum

| Platform | Paket | Durum |
|----------|-------|-------|
| npm | @hooksniff/sdk | ⏳ Reserve edilecek |
| npm | @hooksniff/cli | ⏳ Reserve edilecek |
| PyPI | hooksniff | ⏳ Reserve edilecek |
| crates.io | hooksniff | ⏳ Reserve edilecek |
| Terraform | hooksniff | ⏳ Oluşturulacak |
