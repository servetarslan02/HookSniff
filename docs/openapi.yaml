openapi: 3.0.3
info:
  title: HookSniff API
  version: 1.0.0
  description: 'Webhook delivery, monitoring, and management API.

    All endpoints under `/v1` require authentication via `Authorization: Bearer <api_key>` header

    unless marked as **Public**.

    '
  contact:
    name: HookSniff Support
    email: support@hooksniff.vercel.app
    url: https://hooksniff.vercel.app
  license:
    name: Proprietary
servers:
- url: https://hooksniff-api-1046140057667.europe-west1.run.app/v1
  description: Production (Google Cloud Run)
- url: http://localhost:3000/v1
  description: Local development
tags:
- name: Auth
  description: Registration, login, password reset, email verification, 2FA
- name: Endpoints
  description: Manage webhook destination endpoints
- name: Webhooks
  description: Send and manage webhook deliveries
- name: API Keys
  description: Manage API keys
- name: Alerts
  description: Alert rules and notifications
- name: Analytics
  description: Delivery trends, success rates, latency metrics
- name: Stats
  description: Account usage statistics
- name: Search
  description: Search webhook deliveries
- name: Inbound
  description: Receive webhooks from external providers
- name: Notifications
  description: In-app notification management
- name: Devices
  description: Push notification device tokens
- name: Teams
  description: Team management and invitations
- name: Billing
  description: Subscription, usage, invoices, payment webhooks
- name: Templates
  description: Webhook payload templates
- name: Schemas
  description: JSON Schema registry and validation
- name: Routing
  description: Smart routing configuration per endpoint
- name: Playground
  description: Test webhooks in sandbox
- name: Transforms
  description: Payload transformation rules
- name: Stream
  description: Real-time delivery event stream (SSE)
- name: Admin
  description: Admin-only user and system management
- name: Contact
  description: Contact form submission
- name: Outbound IPs
  description: Static IPs for firewall whitelisting
- name: Health
  description: System health and status
- name: Customer Portal
  description: Self-service customer portal
- name: Delivery Details
  description: Detailed delivery and attempt info
- name: Audit Log
  description: Audit trail for all account actions
- name: SSO
  description: Single Sign-On configuration (SAML/OIDC)
- name: Custom Domains
  description: Custom domain management
- name: OAuth
  description: OAuth/SSO login providers
- name: Rate Limits
  description: Per-endpoint rate limiting
- name: Embed
  description: Embeddable customer portal widget
- name: Simulator
  description: Webhook simulator for testing
- name: Events
  description: Event type management
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      description: API key passed as Bearer token
  schemas:
    Error:
      type: object
      properties:
        error:
          type: string
          description: Human-readable error message
      required:
      - error
    Uuid:
      type: string
      format: uuid
    DateTime:
      type: string
      format: date-time
    RegisterRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
        name:
          type: string
      required:
      - email
    LoginRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
      required:
      - email
      - password
    LogoutRequest:
      type: object
      description: Optional request body for explicit refresh token invalidation
      properties:
        refresh_token:
          type: string
          description: Refresh token to invalidate
      required:
      - refresh_token
    AuthResponse:
      type: object
      properties:
        token:
          type: string
          description: JWT access token
        customer:
          $ref: '#/components/schemas/CustomerResponse'
        refresh_token:
          type: string
          description: Refresh token (when applicable)
      required:
      - token
      - customer
    TwoFactorRequiredResponse:
      type: object
      properties:
        requires_2fa:
          type: boolean
          example: true
        temp_token:
          type: string
        message:
          type: string
      required:
      - requires_2fa
      - temp_token
      - message
    CustomerResponse:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        email:
          type: string
        name:
          type: string
          nullable: true
        api_key:
          type: string
          description: Only returned on registration
          nullable: true
        plan:
          type: string
          enum:
          - free
          - pro
          - business
        webhook_limit:
          type: integer
        webhook_count:
          type: integer
        is_admin:
          type: boolean
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - email
      - plan
      - webhook_limit
      - webhook_count
      - is_admin
      - created_at
    ForgotPasswordRequest:
      type: object
      properties:
        email:
          type: string
          format: email
      required:
      - email
    ResetPasswordRequest:
      type: object
      properties:
        token:
          type: string
        new_password:
          type: string
          minLength: 8
      required:
      - token
      - new_password
    VerifyEmailRequest:
      type: object
      properties:
        token:
          type: string
      required:
      - token
    ResendVerificationRequest:
      type: object
      properties:
        email:
          type: string
          format: email
      required:
      - email
    RefreshTokenRequest:
      type: object
      properties:
        refresh_token:
          type: string
      required:
      - refresh_token
    Verify2faRequest:
      type: object
      properties:
        temp_token:
          type: string
        code:
          type: string
          minLength: 6
          maxLength: 6
      required:
      - temp_token
      - code
    Enable2faRequest:
      type: object
      properties:
        password:
          type: string
      required:
      - password
    Confirm2faRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 6
          maxLength: 6
      required:
      - code
    Disable2faRequest:
      type: object
      properties:
        password:
          type: string
      required:
      - password
    Enable2faResponse:
      type: object
      description: TOTP secret and QR code URL returned after enabling 2FA
      properties:
        secret:
          type: string
          description: TOTP secret key
        qr_url:
          type: string
          format: uri
          description: QR code provisioning URL
      required:
      - secret
      - qr_url
    ExportDataResponse:
      type: object
      description: GDPR data export containing all user data
      properties:
        user:
          $ref: '#/components/schemas/CustomerResponse'
        endpoints:
          type: array
          items:
            $ref: '#/components/schemas/Endpoint'
        deliveries:
          type: array
          items:
            $ref: '#/components/schemas/Delivery'
        teams:
          type: array
          items:
            $ref: '#/components/schemas/Team'
        exported_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - exported_at
    UpdateProfileRequest:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
          format: email
      required:
      - name
      - email
    ChangePasswordRequest:
      type: object
      properties:
        current_password:
          type: string
        new_password:
          type: string
          minLength: 8
      required:
      - current_password
      - new_password
    Endpoint:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        url:
          type: string
          format: uri
        description:
          type: string
          nullable: true
        is_active:
          type: boolean
        retry_policy:
          $ref: '#/components/schemas/RetryPolicy'
        created_at:
          $ref: '#/components/schemas/DateTime'
        allowed_ips:
          type: array
          items:
            type: string
          nullable: true
          description: CIDR blocks or exact IPs
        event_filter:
          type: array
          items:
            type: string
          nullable: true
          description: Wildcard patterns (e.g. "order.*")
        custom_headers:
          type: object
          nullable: true
        routing_strategy:
          type: string
          enum:
          - round-robin
          - latency
          - failover
        fallback_url:
          type: string
          format: uri
          nullable: true
        avg_response_ms:
          type: integer
        failure_streak:
          type: integer
        format:
          type: string
          enum:
          - standard
          - cloudevents
      required:
      - id
      - url
      - is_active
      - retry_policy
      - created_at
      - routing_strategy
      - avg_response_ms
      - failure_streak
      - format
    EndpointListResponse:
      type: object
      description: Paginated list of endpoints
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Endpoint'
        total:
          type: integer
        has_more:
          type: boolean
      required:
      - data
      - total
      - has_more
    CreateEndpointRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
        description:
          type: string
        allowed_ips:
          type: array
          items:
            type: string
        event_filter:
          type: array
          items:
            type: string
        custom_headers:
          type: object
        retry_policy:
          $ref: '#/components/schemas/RetryPolicy'
        routing_strategy:
          type: string
          enum:
          - round-robin
          - latency
          - failover
        fallback_url:
          type: string
          format: uri
        format:
          type: string
          enum:
          - standard
          - cloudevents
          default: standard
      required:
      - url
    UpdateEndpointRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
        description:
          type: string
        is_active:
          type: boolean
        allowed_ips:
          type: array
          items:
            type: string
        event_filter:
          type: array
          items:
            type: string
        custom_headers:
          type: object
        retry_policy:
          $ref: '#/components/schemas/RetryPolicy'
        routing_strategy:
          type: string
          enum:
          - round-robin
          - latency
          - failover
        fallback_url:
          type: string
          format: uri
        format:
          type: string
          enum:
          - standard
          - cloudevents
      required:
      - url
      - description
      - is_active
      - allowed_ips
      - event_filter
      - retry_policy
      - routing_strategy
      - fallback_url
      - format
    RetryPolicy:
      type: object
      properties:
        max_attempts:
          type: integer
          default: 3
        backoff:
          type: string
          enum:
          - exponential
          - linear
          - fixed
          default: exponential
        initial_delay_secs:
          type: integer
          default: 10
        max_delay_secs:
          type: integer
          default: 3600
      required:
      - max_attempts
      - backoff
      - initial_delay_secs
      - max_delay_secs
    RotateSecretResponse:
      type: object
      description: New signing secret after rotation
      properties:
        secret:
          type: string
          description: New endpoint signing secret
      required:
      - secret
    CreateWebhookRequest:
      type: object
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        event:
          type: string
          description: Event type (e.g. "order.created")
        data:
          type: object
          description: Webhook payload
      required:
      - endpoint_id
      - data
    BatchWebhookRequest:
      type: object
      properties:
        webhooks:
          type: array
          items:
            $ref: '#/components/schemas/CreateWebhookRequest'
      required:
      - webhooks
    Delivery:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        event:
          type: string
          nullable: true
        status:
          type: string
          enum:
          - pending
          - processing
          - delivered
          - failed
        attempt_count:
          type: integer
        response_status:
          type: integer
          nullable: true
        replay_count:
          type: integer
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - endpoint_id
      - status
      - attempt_count
      - replay_count
      - created_at
    DeliveryListResponse:
      type: object
      properties:
        deliveries:
          type: array
          items:
            $ref: '#/components/schemas/Delivery'
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer
      required:
      - deliveries
      - total
      - page
      - per_page
    BatchResponse:
      type: object
      properties:
        deliveries:
          type: array
          items:
            $ref: '#/components/schemas/Delivery'
        errors:
          type: array
          items:
            type: object
            properties:
              index:
                type: integer
              error:
                type: string
      required:
      - deliveries
      - errors
    DeliveryAttempt:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        attempt_number:
          type: integer
        status_code:
          type: integer
          nullable: true
        response_body:
          type: string
          nullable: true
        duration_ms:
          type: integer
          nullable: true
        error_message:
          type: string
          nullable: true
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - attempt_number
      - created_at
    BatchReplayRequest:
      type: object
      properties:
        ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
      required:
      - ids
    ApiKeyInfo:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        prefix:
          type: string
          description: Masked key prefix (e.g. "hs_abc1...")
        created_at:
          $ref: '#/components/schemas/DateTime'
        last_used_at:
          type: string
          nullable: true
        is_active:
          type: boolean
      required:
      - id
      - prefix
      - created_at
      - is_active
    CreateApiKeyResponse:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        key:
          type: string
          description: Full API key — only shown once
        prefix:
          type: string
        message:
          type: string
      required:
      - id
      - key
      - prefix
      - message
    AlertRule:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        condition:
          type: string
          enum:
          - failure_rate
          - latency
          - consecutive_failures
        threshold:
          type: integer
        channels:
          type: array
          items:
            type: string
            enum:
            - slack
            - email
            - webhook
        is_active:
          type: boolean
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - name
      - condition
      - threshold
      - channels
      - is_active
      - created_at
    CreateAlertRequest:
      type: object
      properties:
        name:
          type: string
        condition:
          type: string
          enum:
          - failure_rate
          - latency
          - consecutive_failures
        threshold:
          type: integer
        channels:
          type: array
          items:
            type: string
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
      required:
      - name
      - condition
      - threshold
      - channels
    DeliveryTrendResponse:
      type: object
      properties:
        range:
          type: string
        buckets:
          type: array
          items:
            type: object
            properties:
              timestamp:
                type: string
              successful:
                type: integer
              failed:
                type: integer
              total:
                type: integer
      required:
      - range
      - buckets
    SuccessRateResponse:
      type: object
      properties:
        range:
          type: string
        successful:
          type: integer
        failed:
          type: integer
        pending:
          type: integer
        success_rate:
          type: number
          format: double
      required:
      - range
      - successful
      - failed
      - pending
      - success_rate
    LatencyTrendResponse:
      type: object
      properties:
        range:
          type: string
        buckets:
          type: array
          items:
            type: object
            properties:
              timestamp:
                type: string
              avg_ms:
                type: number
              p95_ms:
                type: number
        overall_avg_ms:
          type: number
      required:
      - range
      - buckets
      - overall_avg_ms
    StatsResponse:
      type: object
      properties:
        total_deliveries:
          type: integer
        successful_deliveries:
          type: integer
        failed_deliveries:
          type: integer
        total_endpoints:
          type: integer
        active_endpoints:
          type: integer
        plan:
          type: string
        webhook_limit:
          type: integer
        webhook_count:
          type: integer
      required:
      - total_deliveries
      - successful_deliveries
      - failed_deliveries
      - total_endpoints
      - active_endpoints
      - plan
      - webhook_limit
      - webhook_count
    SearchResult:
      type: object
      properties:
        deliveries:
          type: array
          items:
            $ref: '#/components/schemas/Delivery'
        total:
          type: integer
      required:
      - deliveries
      - total
    Notification:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        title:
          type: string
        body:
          type: string
        is_read:
          type: boolean
        link:
          type: string
          nullable: true
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - title
      - body
      - is_read
      - created_at
    NotificationListResponse:
      type: object
      properties:
        notifications:
          type: array
          items:
            $ref: '#/components/schemas/Notification'
        total:
          type: integer
        unread_count:
          type: integer
      required:
      - notifications
      - total
      - unread_count
    RegisterDeviceRequest:
      type: object
      properties:
        token:
          type: string
          description: FCM device token
        platform:
          type: string
          enum:
          - android
          - ios
          - web
      required:
      - token
    DeviceTokenResponse:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        token:
          type: string
        platform:
          type: string
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - token
      - platform
      - created_at
    Team:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - name
      - created_at
    CreateTeamRequest:
      type: object
      properties:
        name:
          type: string
      required:
      - name
    InviteRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        role:
          type: string
          enum:
          - admin
          - member
          - viewer
      required:
      - email
    ChangeRoleRequest:
      type: object
      properties:
        role:
          type: string
          enum:
          - admin
          - member
          - viewer
      required:
      - role
    TeamDetailResponse:
      type: object
      properties:
        team:
          $ref: '#/components/schemas/Team'
        members:
          type: array
          items:
            $ref: '#/components/schemas/TeamMember'
        invites:
          type: array
          items:
            $ref: '#/components/schemas/TeamInvite'
      required:
      - team
      - members
      - invites
    TeamMember:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        user_id:
          $ref: '#/components/schemas/Uuid'
        email:
          type: string
        name:
          type: string
          nullable: true
        role:
          type: string
        joined_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - user_id
      - email
      - role
      - joined_at
    TeamInvite:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        email:
          type: string
        role:
          type: string
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - email
      - role
      - created_at
    SubscriptionResponse:
      type: object
      properties:
        plan:
          type: string
        status:
          type: string
        payment_provider:
          type: string
        webhook_limit:
          type: integer
        endpoint_limit:
          type: integer
        retention_days:
          type: integer
        monthly_price_cents:
          type: integer
          format: int64
      required:
      - plan
      - status
      - payment_provider
      - webhook_limit
      - endpoint_limit
      - retention_days
      - monthly_price_cents
    UpgradeRequest:
      type: object
      properties:
        plan:
          type: string
          enum:
          - pro
          - business
        provider:
          type: string
          enum:
          - stripe
          - polar
          - iyzico
      required:
      - plan
    UpgradeResponse:
      type: object
      properties:
        checkout_url:
          type: string
          nullable: true
        provider:
          type: string
        message:
          type: string
      required:
      - provider
      - message
    UsageResponse:
      type: object
      properties:
        plan:
          type: string
        period_start:
          $ref: '#/components/schemas/DateTime'
        period_end:
          $ref: '#/components/schemas/DateTime'
        webhooks_used:
          type: integer
        webhooks_limit:
          type: integer
        endpoints_used:
          type: integer
        endpoints_limit:
          type: integer
      required:
      - plan
      - period_start
      - period_end
      - webhooks_used
      - webhooks_limit
      - endpoints_used
      - endpoints_limit
    InvoiceResponse:
      type: object
      properties:
        id:
          type: string
        amount_cents:
          type: integer
          format: int64
        currency:
          type: string
        status:
          type: string
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - amount_cents
      - currency
      - status
      - created_at
    WebhookTemplate:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        category:
          type: string
        payload_template:
          type: object
      required:
      - id
      - name
      - description
      - category
    ApplyTemplateRequest:
      type: object
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        variables:
          type: object
      required:
      - endpoint_id
    ApplyTemplateResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
      required:
      - success
      - message
    RegisterSchemaRequest:
      type: object
      properties:
        name:
          type: string
        schema:
          type: object
          description: JSON Schema document
      required:
      - name
      - schema
    ValidateEventRequest:
      type: object
      properties:
        event:
          type: object
      required:
      - event
    RoutingInfo:
      type: object
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        routing_strategy:
          type: string
        fallback_url:
          type: string
          nullable: true
        avg_response_ms:
          type: integer
        failure_streak:
          type: integer
        is_healthy:
          type: boolean
      required:
      - endpoint_id
      - routing_strategy
      - avg_response_ms
      - failure_streak
      - is_healthy
    UpdateRoutingRequest:
      type: object
      properties:
        routing_strategy:
          type: string
          enum:
          - round-robin
          - latency
          - failover
        fallback_url:
          type: string
          format: uri
      required:
      - routing_strategy
      - fallback_url
    EndpointHealth:
      type: object
      description: Endpoint health metrics and status
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        is_healthy:
          type: boolean
        failure_streak:
          type: integer
        avg_response_ms:
          type: integer
        last_failure_at:
          $ref: '#/components/schemas/DateTime'
          nullable: true
        success_rate:
          type: number
          format: double
          description: Success rate as a fraction (0.0–1.0)
        avg_latency_ms:
          type: number
          description: Average delivery latency in milliseconds
        last_delivery_at:
          $ref: '#/components/schemas/DateTime'
          nullable: true
        total_deliveries:
          type: integer
        failed_deliveries:
          type: integer
      required:
      - endpoint_id
      - is_healthy
    TestWebhookRequest:
      type: object
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        payload:
          type: object
        event:
          type: string
      required:
      - endpoint_id
      - payload
    TestWebhookResponse:
      type: object
      properties:
        success:
          type: boolean
        status_code:
          type: integer
        duration_ms:
          type: integer
        response_body:
          type: string
      required:
      - success
      - status_code
      - duration_ms
      - response_body
    TransformRule:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        rule_type:
          type: string
        config:
          type: object
        is_active:
          type: boolean
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - endpoint_id
      - name
      - rule_type
      - is_active
      - created_at
    CreateTransformRuleRequest:
      type: object
      properties:
        name:
          type: string
        rule_type:
          type: string
        config:
          type: object
      required:
      - name
      - rule_type
      - config
    ContactRequest:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
          format: email
        subject:
          type: string
        message:
          type: string
      required:
      - name
      - email
      - subject
      - message
    ContactResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
      required:
      - success
      - message
    OutboundIpsResponse:
      type: object
      properties:
        ips:
          type: array
          items:
            type: string
        updated_at:
          type: string
      required:
      - ips
      - updated_at
    SystemStatus:
      type: object
      properties:
        overall_status:
          type: string
          enum:
          - operational
          - degraded
          - down
        uptime_30d:
          type: number
        components:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              status:
                type: string
              latency_ms:
                type: integer
                nullable: true
              description:
                type: string
              last_checked:
                type: string
        checked_at:
          type: string
      required:
      - overall_status
      - uptime_30d
      - components
      - checked_at
    NotificationPreferences:
      type: object
      properties:
        email_on_failure:
          type: boolean
          default: true
        email_on_dead_letter:
          type: boolean
          default: true
        email_on_success:
          type: boolean
          default: false
        slack_webhook_url:
          type: string
          nullable: true
        discord_webhook_url:
          type: string
          nullable: true
        webhook_url:
          type: string
          nullable: true
      required:
      - email_on_failure
      - email_on_dead_letter
      - email_on_success
    UpdateNotificationPreferences:
      type: object
      properties:
        email_on_failure:
          type: boolean
        email_on_dead_letter:
          type: boolean
        email_on_success:
          type: boolean
        slack_webhook_url:
          type: string
          nullable: true
        discord_webhook_url:
          type: string
          nullable: true
        webhook_url:
          type: string
          nullable: true
      required:
      - email_on_failure
      - email_on_dead_letter
      - email_on_success
    PortalProfile:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        email:
          type: string
        name:
          type: string
          nullable: true
        plan:
          type: string
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - email
      - plan
      - created_at
    PaginatedUsers:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: '#/components/schemas/UserSummary'
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer
      required:
      - users
      - total
      - page
      - per_page
    UserSummary:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        email:
          type: string
        name:
          type: string
          nullable: true
        plan:
          type: string
        is_active:
          type: boolean
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - email
      - plan
      - is_active
      - created_at
    SystemStats:
      type: object
      properties:
        total_users:
          type: integer
        active_users:
          type: integer
        total_endpoints:
          type: integer
        total_deliveries:
          type: integer
        plan_breakdown:
          type: array
          items:
            type: object
            properties:
              plan:
                type: string
              count:
                type: integer
      required:
      - total_users
      - active_users
      - total_endpoints
      - total_deliveries
      - plan_breakdown
    StreamParams:
      type: object
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - pending
          - processing
          - delivered
          - failed
        limit:
          type: integer
          default: 50
      required:
      - endpoint_id
      - status
      - limit
    DeliveryDetailResponse:
      type: object
      description: Full delivery detail including all retry attempts and endpoint info
      properties:
        delivery:
          $ref: '#/components/schemas/Delivery'
        attempts:
          type: array
          items:
            $ref: '#/components/schemas/DeliveryAttempt'
        endpoint:
          $ref: '#/components/schemas/Endpoint'
        request_headers:
          type: object
          nullable: true
          description: Original request headers sent with the delivery
        request_body:
          type: object
          nullable: true
          description: Original request body sent with the delivery
        response_headers:
          type: object
          nullable: true
          description: Response headers received from the endpoint
      required:
      - delivery
      - attempts
    WebhookFilter:
      type: object
      description: Query parameters for filtering webhook deliveries
      properties:
        status:
          type: string
          enum:
          - pending
          - processing
          - delivered
          - failed
          description: Filter by delivery status
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        event_type:
          type: string
          description: Filter by event type (e.g. order.created)
        from_date:
          $ref: '#/components/schemas/DateTime'
        to_date:
          $ref: '#/components/schemas/DateTime'
        page:
          type: integer
          default: 1
        per_page:
          type: integer
          default: 20
      required:
      - status
      - endpoint_id
      - event_type
      - from_date
      - to_date
      - page
      - per_page
    BatchWebhookResponse:
      type: object
      description: Response for batch webhook delivery creation
      properties:
        delivery_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
          description: List of created delivery IDs
        count:
          type: integer
          description: Number of deliveries created
      required:
      - delivery_ids
      - count
    DeliveryAttemptListResponse:
      type: object
      description: Paginated list of delivery attempts
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DeliveryAttempt'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    BillingPortalResponse:
      type: object
      description: URL for the customer billing portal
      properties:
        url:
          type: string
          format: uri
          description: Stripe billing portal URL
      required:
      - url
    CancelSubscriptionRequest:
      type: object
      description: Request to cancel current subscription
      properties:
        reason:
          type: string
          description: Optional reason for cancellation
      required:
      - reason
    CancelSubscriptionResponse:
      type: object
      description: Result of subscription cancellation
      properties:
        cancelled_at:
          $ref: '#/components/schemas/DateTime'
        ends_at:
          $ref: '#/components/schemas/DateTime'
          description: Date when access ends (end of billing period)
      required:
      - cancelled_at
      - ends_at
    InvoiceListResponse:
      type: object
      description: Paginated list of invoices
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/InvoiceResponse'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    UpdateSubscriptionRequest:
      type: object
      description: Request to change subscription plan
      properties:
        plan:
          type: string
          enum:
          - free
          - pro
          - business
          description: Target plan name
        proration:
          type: boolean
          default: true
          description: Whether to prorate charges for the current billing period
      required:
      - plan
    AlertRuleListResponse:
      type: object
      description: Paginated list of alert rules
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AlertRule'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    CreateAlertRuleRequest:
      type: object
      description: Request to create a new alert rule
      properties:
        name:
          type: string
          description: Human-readable alert name
        condition:
          type: string
          enum:
          - failure_rate
          - latency
          - consecutive_failures
          description: Condition that triggers the alert
        threshold:
          type: integer
          description: Threshold value for the condition
        channels:
          type: array
          items:
            type: string
            enum:
            - slack
            - email
            - webhook
          description: Notification channels to alert on
      required:
      - name
      - condition
      - threshold
      - channels
    UpdateAlertRuleRequest:
      type: object
      description: Request to update an existing alert rule (all fields optional)
      properties:
        name:
          type: string
        condition:
          type: string
          enum:
          - failure_rate
          - latency
          - consecutive_failures
        threshold:
          type: integer
        channels:
          type: array
          items:
            type: string
            enum:
            - slack
            - email
            - webhook
      required:
      - name
      - condition
      - threshold
      - channels
    AlertNotificationListResponse:
      type: object
      description: Paginated list of alert notifications
      properties:
        data:
          type: array
          items:
            type: object
            description: Alert notification entry
            properties:
              id:
                $ref: '#/components/schemas/Uuid'
              alert_rule_id:
                $ref: '#/components/schemas/Uuid'
              message:
                type: string
              channel:
                type: string
              status:
                type: string
                enum:
                - sent
                - failed
                - pending
              created_at:
                $ref: '#/components/schemas/DateTime'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    AnalyticsTrendResponse:
      type: object
      description: Delivery trend data over a time period
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AnalyticsTrendPoint'
          description: Array of trend data points
        period:
          type: string
          enum:
          - 24h
          - 7d
          - 30d
          description: Time range of the data
      required:
      - data
      - period
    AnalyticsTrendPoint:
      type: object
      description: Single data point in a delivery trend
      properties:
        date:
          type: string
          format: date
          description: Date of the data point
        total:
          type: integer
          description: Total deliveries on this date
        successful:
          type: integer
          description: Successfully delivered on this date
        failed:
          type: integer
          description: Failed deliveries on this date
        avg_latency_ms:
          type: number
          format: float
          description: Average delivery latency in milliseconds
      required:
      - date
      - total
      - successful
      - failed
    LatencyResponse:
      type: object
      description: Latency percentile breakdown for deliveries
      properties:
        p50:
          type: number
          format: float
          description: 50th percentile (median) latency in ms
        p90:
          type: number
          format: float
          description: 90th percentile latency in ms
        p95:
          type: number
          format: float
          description: 95th percentile latency in ms
        p99:
          type: number
          format: float
          description: 99th percentile latency in ms
        period:
          type: string
          enum:
          - 24h
          - 7d
          - 30d
          description: Time range of the data
      required:
      - p50
      - p90
      - p95
      - p99
      - period
    UsageStatsResponse:
      type: object
      description: Account usage statistics summary
      properties:
        endpoints_count:
          type: integer
          description: Number of active endpoints
        deliveries_count:
          type: integer
          description: Total deliveries in current period
        teams_count:
          type: integer
          description: Number of teams
        storage_used_bytes:
          type: integer
          description: Storage used in bytes
      required:
      - endpoints_count
      - deliveries_count
      - teams_count
      - storage_used_bytes
    SearchRequest:
      type: object
      description: Search request for webhook deliveries
      properties:
        query:
          type: string
          description: Search query string
        filters:
          type: object
          description: Additional filters (status, endpoint_id, etc.)
          properties:
            status:
              type: string
            endpoint_id:
              $ref: '#/components/schemas/Uuid'
        page:
          type: integer
          default: 1
        per_page:
          type: integer
          default: 20
      required:
      - query
    SearchResponse:
      type: object
      description: Search results for webhook deliveries
      properties:
        deliveries:
          type: array
          items:
            $ref: '#/components/schemas/Delivery'
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer
        has_more:
          type: boolean
      required:
      - deliveries
      - total
      - page
      - per_page
    InboundWebhookRequest:
      type: object
      description: Raw webhook payload received from an external provider (Stripe, GitHub, etc.)
      properties:
        provider:
          type: string
          description: Provider name (e.g. stripe, github, shopify)
        payload:
          type: object
          description: Raw webhook payload body
        headers:
          type: object
          description: HTTP headers from the incoming webhook request
      required:
      - provider
      - payload
    InboundWebhookResponse:
      type: object
      description: Result of processing an inbound webhook
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - accepted
          - rejected
          - processing
          description: Processing status of the inbound webhook
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
          description: The endpoint this webhook was routed to
        received_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - status
      - endpoint_id
      - received_at
    TeamListResponse:
      type: object
      description: Paginated list of teams
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Team'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    UpdateTeamRequest:
      type: object
      description: Fields to update on a team (all optional)
      properties:
        name:
          type: string
        description:
          type: string
      required:
      - name
      - description
    TeamMemberListResponse:
      type: object
      description: List of members in a team
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TeamMember'
      required:
      - data
    InviteMemberRequest:
      type: object
      description: Invite a new member to a team
      properties:
        email:
          type: string
          format: email
        role:
          type: string
          enum:
          - admin
          - member
          - viewer
      required:
      - email
      - role
    DeviceListResponse:
      type: object
      description: Paginated list of registered devices
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DeviceTokenResponse'
      required:
      - data
    TemplateListResponse:
      type: object
      description: Paginated list of webhook payload templates
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/WebhookTemplate'
      required:
      - data
    SchemaResponse:
      type: object
      description: A registered JSON Schema for event validation
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        version:
          type: integer
        schema_json:
          type: object
          description: The JSON Schema document
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - name
      - version
      - schema_json
      - created_at
    SchemaListResponse:
      type: object
      description: Paginated list of registered schemas
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/SchemaResponse'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    ValidateEventResponse:
      type: object
      description: Result of validating an event payload against a schema
      properties:
        valid:
          type: boolean
        errors:
          type: array
          items:
            type: object
            properties:
              path:
                type: string
                description: JSON pointer to the failing field
              message:
                type: string
                description: Human-readable validation error
      required:
      - valid
    RoutingRuleListResponse:
      type: object
      description: List of routing rules
      properties:
        data:
          type: array
          items:
            type: object
            properties:
              id:
                $ref: '#/components/schemas/Uuid'
              name:
                type: string
              conditions:
                type: object
              transform:
                type: object
                nullable: true
              target_endpoint_id:
                $ref: '#/components/schemas/Uuid'
              enabled:
                type: boolean
              created_at:
                $ref: '#/components/schemas/DateTime'
            required:
            - id
            - name
            - conditions
            - target_endpoint_id
      required:
      - data
    CreateRoutingRuleRequest:
      type: object
      description: Create a new routing rule
      properties:
        name:
          type: string
        conditions:
          type: object
          description: Conditions that trigger this rule (e.g. event_type, header match)
        transform:
          type: object
          nullable: true
          description: Optional payload transformation config
        target_endpoint_id:
          $ref: '#/components/schemas/Uuid'
      required:
      - name
      - conditions
      - target_endpoint_id
    UpdateRoutingRuleRequest:
      type: object
      description: Update an existing routing rule (all fields optional)
      properties:
        name:
          type: string
        conditions:
          type: object
        transform:
          type: object
          nullable: true
      required:
      - name
    PortalConfig:
      type: object
      description: Customer-facing portal branding and configuration
      properties:
        logo_url:
          type: string
          format: uri
          nullable: true
        primary_color:
          type: string
          nullable: true
          description: Hex color code (e.g.
        custom_domain:
          type: string
          nullable: true
        webhook_events:
          type: array
          items:
            type: string
          description: Event types to expose in the portal
      required: []
    PortalSession:
      type: object
      description: Temporary session token for the customer portal
      properties:
        token:
          type: string
        expires_at:
          $ref: '#/components/schemas/DateTime'
        url:
          type: string
          format: uri
          description: Full URL to the portal with session token
      required:
      - token
      - expires_at
      - url
    AdminUserListResponse:
      type: object
      description: Paginated list of users for admin management
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/UserSummary'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    AdminSystemStatus:
      type: object
      description: System-level status for admin dashboard
      properties:
        version:
          type: string
        uptime_seconds:
          type: integer
        db_status:
          type: string
          enum:
          - healthy
          - degraded
          - down
        redis_status:
          type: string
          enum:
          - healthy
          - degraded
          - down
        queue_depth:
          type: integer
          description: Number of pending jobs in the delivery queue
      required:
      - version
      - uptime_seconds
      - db_status
      - redis_status
      - queue_depth
    AdminRevenueEntry:
      type: object
      description: Monthly revenue data point
      properties:
        date:
          type: string
          format: date
        mrr:
          type: number
          format: double
          description: Monthly recurring revenue in dollars
        new_subscriptions:
          type: integer
        churns:
          type: integer
      required:
      - date
      - mrr
      - new_subscriptions
      - churns
    AdminRevenueResponse:
      type: object
      description: Revenue history for admin analytics
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AdminRevenueEntry'
        total_mrr:
          type: number
          format: double
          description: Current total MRR across all subscriptions
      required:
      - data
      - total_mrr
    AuditLogEntry:
      type: object
      description: A single audit log record
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        actor:
          type: string
          description: Who performed the action (user id or email)
        action:
          type: string
          description: The action taken (e.g. endpoint.create, team.invite)
        resource_type:
          type: string
          description: Type of resource affected (endpoint, team, api_key, etc.)
        resource_id:
          type: string
          description: ID of the affected resource
        timestamp:
          $ref: '#/components/schemas/DateTime'
        metadata:
          type: object
          nullable: true
          description: Additional context (old_value, new_value, ip, etc.)
      required:
      - id
      - actor
      - action
      - resource_type
      - resource_id
      - timestamp
    AuditLogListResponse:
      type: object
      description: Paginated list of audit log entries
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AuditLogEntry'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    SSOConfig:
      type: object
      description: Single Sign-On configuration (SAML or OIDC)
      properties:
        provider:
          type: string
          enum:
          - saml
          - oidc
        domain:
          type: string
          description: Email domain for SSO routing
        entity_id:
          type: string
          nullable: true
          description: SAML entity ID or OIDC issuer
        sso_url:
          type: string
          format: uri
          nullable: true
        certificate:
          type: string
          nullable: true
          description: PEM-encoded X.509 certificate (SAML)
      required:
      - provider
      - domain
    SSOConfigListResponse:
      type: object
      description: List of SSO configurations for the account
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/SSOConfig'
      required:
      - data
    CreateSSOConfigRequest:
      type: object
      description: Create a new SSO configuration
      properties:
        provider:
          type: string
          enum:
          - saml
          - oidc
        domain:
          type: string
        metadata_url:
          type: string
          format: uri
          description: URL to SAML metadata or OIDC discovery document
      required:
      - provider
      - domain
      - metadata_url
    UpdateSSOConfigRequest:
      type: object
      description: Update an SSO configuration (all fields optional)
      properties:
        provider:
          type: string
          enum:
          - saml
          - oidc
        domain:
          type: string
      required:
      - provider
      - domain
    CustomDomain:
      type: object
      description: A custom domain configured for the account
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        domain:
          type: string
          description: The custom domain (e.g. webhooks.example.com)
        status:
          type: string
          enum:
          - pending
          - verifying
          - verified
          - failed
        verification_token:
          type: string
          nullable: true
          description: TXT record value to prove domain ownership
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - domain
      - status
      - created_at
    CustomDomainListResponse:
      type: object
      description: List of custom domains
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CustomDomain'
      required:
      - data
    CreateCustomDomainRequest:
      type: object
      description: Register a new custom domain
      properties:
        domain:
          type: string
          description: Fully qualified domain name to register
      required:
      - domain
    VerifyCustomDomainResponse:
      type: object
      description: Result of domain verification attempt
      properties:
        status:
          type: string
          enum:
          - verified
          - pending
          - failed
        dns_records:
          type: array
          items:
            $ref: '#/components/schemas/DomainDnsRecord'
          description: DNS records that need to be configured
      required:
      - status
      - dns_records
    DomainDnsRecord:
      type: object
      description: A DNS record required for domain verification
      properties:
        type:
          type: string
          enum:
          - TXT
          - CNAME
          - A
        name:
          type: string
          description: DNS record name/host
        value:
          type: string
          description: DNS record value
        status:
          type: string
          enum:
          - pending
          - verified
          - failed
      required:
      - type
      - name
      - value
      - status
    RateLimitConfig:
      type: object
      description: Rate limiting configuration for an endpoint
      properties:
        requests_per_second:
          type: integer
          description: Maximum requests per second allowed
        burst_size:
          type: integer
          description: Maximum burst above steady-state rate
        enabled:
          type: boolean
      required:
      - requests_per_second
      - burst_size
      - enabled
    RateLimitUsage:
      type: object
      description: Current rate limit usage for an endpoint
      properties:
        current_rps:
          type: number
          format: double
          description: Current requests per second being consumed
        limit_rps:
          type: number
          format: double
          description: Configured requests per second limit
        remaining:
          type: number
          format: double
          description: Remaining capacity
        reset_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - current_rps
      - limit_rps
      - remaining
      - reset_at
    OAuthProvider:
      type: object
      description: An available OAuth identity provider
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          description: Provider identifier (e.g. google, github)
        client_id:
          type: string
        authorize_url:
          type: string
          format: uri
        token_url:
          type: string
          format: uri
      required:
      - id
      - name
      - client_id
      - authorize_url
      - token_url
    OAuthProviderListResponse:
      type: object
      description: List of available OAuth providers
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/OAuthProvider'
      required:
      - data
    OAuthCallbackRequest:
      type: object
      description: OAuth authorization callback parameters
      properties:
        code:
          type: string
          description: Authorization code from the provider
        state:
          type: string
          description: CSRF state token
        redirect_uri:
          type: string
          format: uri
      required:
      - code
      - state
    EventType:
      type: object
      description: A registered event type in the system
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          description: Event type identifier (e.g. order.created)
        description:
          type: string
          nullable: true
        schema_id:
          $ref: '#/components/schemas/Uuid'
          nullable: true
          description: Associated JSON Schema for payload validation
      required:
      - id
      - name
    EventTypeListResponse:
      type: object
      description: Paginated list of event types
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/EventType'
        has_more:
          type: boolean
        total:
          type: integer
      required:
      - data
      - has_more
      - total
    EmbedConfig:
      type: object
      description: Configuration for embedded webhook dashboard
      properties:
        allowed_origins:
          type: array
          items:
            type: string
          description: CORS origins allowed to load the embed
        theme:
          type: object
          properties:
            primary_color:
              type: string
            background_color:
              type: string
            font_family:
              type: string
          description: Visual customization for the embed
        features:
          type: array
          items:
            type: string
          description: Enabled features (e.g. [deliveries, endpoints, playground])
      required:
      - allowed_origins
    SimulatorRequest:
      type: object
      description: Send a simulated webhook event
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        event_type:
          type: string
          description: Event type to simulate (e.g. order.created)
        payload:
          type: object
          description: The webhook payload to deliver
        delay_ms:
          type: integer
          nullable: true
          description: Artificial delay before delivery (for testing timeouts)
      required:
      - endpoint_id
      - event_type
      - payload
    SimulatorResponse:
      type: object
      description: Result of a simulated webhook delivery
      properties:
        delivery_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - delivered
          - failed
          - pending
        latency_ms:
          type: integer
          description: Response time from the endpoint
      required:
      - delivery_id
      - status
      - latency_ms
    PlaygroundTestRequest:
      type: object
      description: Test a webhook payload against an endpoint in sandbox
      properties:
        endpoint_id:
          $ref: '#/components/schemas/Uuid'
        payload:
          type: object
          description: The payload to send
        headers:
          type: object
          nullable: true
          description: Custom headers to include with the request
      required:
      - endpoint_id
      - payload
    PlaygroundTestResponse:
      type: object
      description: Result of a playground test delivery
      properties:
        status_code:
          type: integer
          description: HTTP status code returned by the endpoint
        response_body:
          type: string
          description: Raw response body from the endpoint
        latency_ms:
          type: integer
        headers:
          type: object
          description: Response headers from the endpoint
      required:
      - status_code
      - response_body
      - latency_ms
    TransformRuleListResponse:
      type: object
      description: Paginated list of transform rules
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TransformRule'
      required:
      - data
    UpdateTransformRuleRequest:
      type: object
      description: Update an existing transform rule (all fields optional)
      properties:
        name:
          type: string
        config:
          type: object
          description: Updated transformation configuration
      required:
      - name
    OutboundIPsResponse:
      type: object
      description: List of static outbound IP addresses for firewall whitelisting
      properties:
        ips:
          type: array
          items:
            type: string
          description: IPv4 and IPv6 addresses used for outbound requests
      required:
      - ips
    AdminAlertRule:
      type: object
      description: Admin alert rule with customer info
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        customer_id:
          $ref: '#/components/schemas/Uuid'
          nullable: true
        customer_email:
          type: string
          nullable: true
        name:
          type: string
        condition:
          type: string
          enum: [failure_rate, latency, consecutive_failures]
        threshold:
          type: integer
        channels:
          type: array
          items:
            type: string
            enum: [slack, email, webhook]
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
      required:
      - id
      - name
      - condition
      - threshold
      - channels
      - is_active
      - created_at
    AdminCreateAlertRequest:
      type: object
      description: Create a platform alert rule (admin)
      properties:
        customer_id:
          $ref: '#/components/schemas/Uuid'
          nullable: true
        name:
          type: string
        condition:
          type: string
          enum: [failure_rate, latency, consecutive_failures]
        threshold:
          type: integer
        channels:
          type: array
          items:
            type: string
            enum: [slack, email, webhook]
      required:
      - name
      - condition
      - threshold
      - channels
    AdminUpdateAlertRequest:
      type: object
      description: Update an alert rule (admin, all fields optional)
      properties:
        name:
          type: string
        condition:
          type: string
          enum: [failure_rate, latency, consecutive_failures]
        threshold:
          type: integer
        channels:
          type: array
          items:
            type: string
            enum: [slack, email, webhook]
        is_active:
          type: boolean
    AdminAuditLogResponse:
      type: object
      description: Paginated admin audit log response
      properties:
        entries:
          type: array
          items:
            $ref: '#/components/schemas/AdminAuditEntry'
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer
      required:
      - entries
      - total
      - page
      - per_page
    AdminAuditEntry:
      type: object
      description: A single admin audit log entry
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        customer_id:
          $ref: '#/components/schemas/Uuid'
        action:
          type: string
        resource_type:
          type: string
        resource_id:
          type: string
          nullable: true
        details:
          type: object
          nullable: true
        ip_address:
          type: string
          nullable: true
        user_agent:
          type: string
          nullable: true
        created_at:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - customer_id
      - action
      - resource_type
      - created_at
    ChurnResponse:
      type: object
      description: Churn report with list of recently churned users
      properties:
        users:
          type: array
          items:
            $ref: '#/components/schemas/ChurnedUser'
      required:
      - users
    ChurnedUser:
      type: object
      description: A user who churned (became inactive) recently
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        email:
          type: string
        name:
          type: string
          nullable: true
        plan:
          type: string
        amount:
          type: number
          format: double
        churn_date:
          $ref: '#/components/schemas/DateTime'
      required:
      - id
      - email
      - plan
      - amount
      - churn_date
    PlatformSettings:
      type: object
      description: Platform-wide configuration settings
      properties:
        default_plan:
          type: string
        max_endpoints_free:
          type: integer
        max_endpoints_pro:
          type: integer
        max_webhooks_free:
          type: integer
        max_webhooks_pro:
          type: integer
        rate_limit_free:
          type: integer
        rate_limit_pro:
          type: integer
        retry_max_attempts:
          type: integer
        retention_days_free:
          type: integer
        retention_days_pro:
          type: integer
        maintenance_mode:
          type: boolean
        signup_enabled:
          type: boolean
        plan_price_pro:
          type: number
          format: double
        plan_price_business:
          type: number
          format: double
        resend_api_key:
          type: string
          nullable: true
        email_sender:
          type: string
          nullable: true
      required:
      - default_plan
      - max_endpoints_free
      - max_endpoints_pro
      - max_webhooks_free
      - max_webhooks_pro
      - rate_limit_free
      - rate_limit_pro
      - retry_max_attempts
      - retention_days_free
      - retention_days_pro
      - maintenance_mode
      - signup_enabled
      - plan_price_pro
      - plan_price_business
    UserAnalytics:
      type: object
      description: User analytics data for admin view
      properties:
        daily_deliveries:
          type: array
          items:
            $ref: '#/components/schemas/DailyDeliveryCount'
        top_events:
          type: array
          items:
            $ref: '#/components/schemas/EventTypeCount'
        endpoint_health:
          type: array
          items:
            $ref: '#/components/schemas/EndpointHealth'
      required:
      - daily_deliveries
      - top_events
      - endpoint_health
    DailyDeliveryCount:
      type: object
      description: Daily delivery count breakdown
      properties:
        date:
          type: string
          format: date
        total:
          type: integer
        success:
          type: integer
        failed:
          type: integer
      required:
      - date
      - total
      - success
      - failed
    EventTypeCount:
      type: object
      description: Event type occurrence count
      properties:
        event:
          type: string
          nullable: true
        count:
          type: integer
      required:
      - count
    RevenueResponse:
      type: object
      description: Full revenue analytics response
      properties:
        monthly_revenue:
          type: array
          items:
            type: object
            properties:
              month:
                type: string
              revenue:
                type: number
                format: double
        revenue_by_plan:
          type: array
          items:
            type: object
            properties:
              plan:
                type: string
              revenue:
                type: number
                format: double
              count:
                type: integer
        mrr:
          type: number
          format: double
        churn_rate:
          type: number
          format: double
        mrr_trend:
          type: number
          format: double
      required:
      - monthly_revenue
      - revenue_by_plan
      - mrr
      - churn_rate
      - mrr_trend
    AdminTestWebhookRequest:
      type: object
      description: Send a test HTTP POST to a URL (admin)
      properties:
        endpoint_url:
          type: string
          format: uri
        event_type:
          type: string
          nullable: true
        payload:
          type: object
      required:
      - endpoint_url
      - payload
    AdminTestWebhookResponse:
      type: object
      description: Result of a test webhook delivery
      properties:
        status_code:
          type: integer
        response_body:
          type: string
        duration_ms:
          type: integer
      required:
      - status_code
      - response_body
      - duration_ms
    ReplayDeliveryResponse:
      type: object
      description: Result of replaying a delivery
      properties:
        message:
          type: string
        original_id:
          $ref: '#/components/schemas/Uuid'
        new_delivery_id:
          $ref: '#/components/schemas/Uuid'
      required:
      - message
      - original_id
      - new_delivery_id
    OAuthLoginRedirect:
      type: object
      description: OAuth redirect information
      properties:
        redirect_url:
          type: string
          format: uri
      required:
      - redirect_url
security:
- BearerAuth: []
paths:
  /auth/register:
    post:
      tags:
      - Auth
      summary: Register a new account
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Account created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Email already exists
  /auth/login:
    post:
      tags:
      - Auth
      summary: Login with email and password
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login successful (or 2FA required)
          content:
            application/json:
              schema:
                oneOf:
                - $ref: '#/components/schemas/AuthResponse'
                - $ref: '#/components/schemas/TwoFactorRequiredResponse'
        '401':
          description: Invalid credentials
  /auth/forgot-password:
    post:
      tags:
      - Auth
      summary: Request password reset email
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ForgotPasswordRequest'
      responses:
        '200':
          description: Reset email sent (if account exists)
  /auth/reset-password:
    post:
      tags:
      - Auth
      summary: Reset password with token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordRequest'
      responses:
        '200':
          description: Password reset successful
        '400':
          description: Invalid or expired token
  /auth/verify-email:
    post:
      tags:
      - Auth
      summary: Verify email address
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyEmailRequest'
      responses:
        '200':
          description: Email verified
        '400':
          description: Invalid token
  /auth/resend-verification:
    post:
      tags:
      - Auth
      summary: Resend verification email
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResendVerificationRequest'
      responses:
        '200':
          description: Verification email sent (if account exists and unverified)
  /auth/refresh:
    post:
      tags:
      - Auth
      summary: Refresh access token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '200':
          description: New tokens issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Invalid refresh token
  /auth/2fa/verify:
    post:
      tags:
      - Auth
      summary: Verify 2FA code during login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Verify2faRequest'
      responses:
        '200':
          description: 2FA verified, full token issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Invalid code
  /auth/me:
    get:
      tags:
      - Auth
      summary: Get current user profile
      responses:
        '200':
          description: Current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerResponse'
  /auth/profile:
    put:
      tags:
      - Auth
      summary: Update profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileRequest'
      responses:
        '200':
          description: Profile updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerResponse'
  /auth/password:
    put:
      tags:
      - Auth
      summary: Change password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '200':
          description: Password changed
        '400':
          description: Current password incorrect
  /auth/2fa/enable:
    post:
      tags:
      - Auth
      summary: Enable 2FA (returns TOTP secret and QR URL)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Enable2faRequest'
      responses:
        '200':
          description: TOTP secret generated
          content:
            application/json:
              schema:
                type: object
                properties:
                  secret:
                    type: string
                  qr_url:
                    type: string
  /auth/2fa/confirm:
    post:
      tags:
      - Auth
      summary: Confirm 2FA setup with a code
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Confirm2faRequest'
      responses:
        '200':
          description: 2FA enabled
        '400':
          description: Invalid code
  /auth/2fa/disable:
    post:
      tags:
      - Auth
      summary: Disable 2FA
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Disable2faRequest'
      responses:
        '200':
          description: 2FA disabled
        '400':
          description: Invalid password
  /endpoints:
    get:
      tags:
      - Endpoints
      summary: List all endpoints
      responses:
        '200':
          description: Endpoint list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Endpoint'
    post:
      tags:
      - Endpoints
      summary: Create a new endpoint
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEndpointRequest'
      responses:
        '201':
          description: Endpoint created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
  /endpoints/{id}:
    get:
      tags:
      - Endpoints
      summary: Get endpoint by ID
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Endpoint details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
        '404':
          description: Not found
    put:
      tags:
      - Endpoints
      summary: Update endpoint
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEndpointRequest'
      responses:
        '200':
          description: Endpoint updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
    delete:
      tags:
      - Endpoints
      summary: Delete endpoint
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Endpoint deleted
  /endpoints/{id}/rotate-secret:
    post:
      tags:
      - Endpoints
      summary: Rotate endpoint signing secret
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Secret rotated
          content:
            application/json:
              schema:
                type: object
                properties:
                  signing_secret:
                    type: string
                  message:
                    type: string
  /endpoints/{id}/retry-policy:
    put:
      tags:
      - Endpoints
      summary: Update retry policy for an endpoint
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RetryPolicy'
      responses:
        '200':
          description: Retry policy updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
  /webhooks:
    get:
      tags:
      - Webhooks
      summary: List webhook deliveries
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 20
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - processing
          - delivered
          - failed
      - name: endpoint_id
        in: query
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Paginated delivery list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryListResponse'
    post:
      tags:
      - Webhooks
      summary: Send a webhook
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWebhookRequest'
      responses:
        '201':
          description: Webhook queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Delivery'
  /webhooks/batch:
    post:
      tags:
      - Webhooks
      summary: Send multiple webhooks in batch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchWebhookRequest'
      responses:
        '201':
          description: Batch processed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchResponse'
  /webhooks/batch/replay:
    post:
      tags:
      - Webhooks
      summary: Replay multiple deliveries by ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchReplayRequest'
      responses:
        '200':
          description: Batch replay initiated
  /webhooks/export:
    get:
      tags:
      - Webhooks
      summary: Export deliveries as CSV
      parameters:
      - name: range
        in: query
        schema:
          type: string
          enum:
          - 24h
          - 7d
          - 30d
          default: 7d
      responses:
        '200':
          description: CSV file
          content:
            text/csv:
              schema:
                type: string
  /webhooks/{id}:
    get:
      tags:
      - Webhooks
      summary: Get delivery by ID
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Delivery details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Delivery'
  /webhooks/{id}/replay:
    post:
      tags:
      - Webhooks
      summary: Replay a single delivery
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Replay initiated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Delivery'
  /webhooks/{id}/attempts:
    get:
      tags:
      - Webhooks
      summary: Get delivery attempts
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Attempt list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DeliveryAttempt'
  /webhooks/{id}/details:
    get:
      tags:
      - Delivery Details
      summary: Get detailed delivery info
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Full delivery details
  /webhooks/{id}/attempts/{attempt_id}:
    get:
      tags:
      - Delivery Details
      summary: Get specific attempt detail
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: attempt_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Attempt details
  /api-keys:
    get:
      tags:
      - API Keys
      summary: List API keys
      responses:
        '200':
          description: API key list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ApiKeyInfo'
    post:
      tags:
      - API Keys
      summary: Create a new API key
      responses:
        '201':
          description: API key created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateApiKeyResponse'
  /api-keys/{id}:
    delete:
      tags:
      - API Keys
      summary: Delete (revoke) an API key
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: API key revoked
  /api-keys/{id}/rotate:
    post:
      tags:
      - API Keys
      summary: Rotate an API key
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: New key generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateApiKeyResponse'
  /alerts:
    get:
      tags:
      - Alerts
      summary: List alert rules
      responses:
        '200':
          description: Alert list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AlertRule'
    post:
      tags:
      - Alerts
      summary: Create alert rule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAlertRequest'
      responses:
        '201':
          description: Alert created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertRule'
  /alerts/{id}:
    get:
      tags:
      - Alerts
      summary: Get alert rule
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Alert details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertRule'
    delete:
      tags:
      - Alerts
      summary: Delete alert rule
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Alert deleted
  /alerts/{id}/test:
    post:
      tags:
      - Alerts
      summary: Test an alert rule
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Test notification sent
  /analytics/deliveries:
    get:
      tags:
      - Analytics
      summary: Delivery trend over time
      parameters:
      - name: range
        in: query
        schema:
          type: string
          enum:
          - 24h
          - 7d
          - 30d
          default: 24h
      responses:
        '200':
          description: Delivery trend data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryTrendResponse'
  /analytics/success-rate:
    get:
      tags:
      - Analytics
      summary: Success rate metrics
      parameters:
      - name: range
        in: query
        schema:
          type: string
          enum:
          - 24h
          - 7d
          - 30d
          default: 24h
      responses:
        '200':
          description: Success rate data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessRateResponse'
  /analytics/latency:
    get:
      tags:
      - Analytics
      summary: Latency trend over time
      parameters:
      - name: range
        in: query
        schema:
          type: string
          enum:
          - 24h
          - 7d
          - 30d
          default: 24h
      responses:
        '200':
          description: Latency trend data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LatencyTrendResponse'
  /stats:
    get:
      tags:
      - Stats
      summary: Get account statistics
      responses:
        '200':
          description: Account stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatsResponse'
  /search:
    get:
      tags:
      - Search
      summary: Search deliveries
      parameters:
      - name: q
        in: query
        required: true
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
      - name: endpoint_id
        in: query
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: page
        in: query
        schema:
          type: integer
      - name: per_page
        in: query
        schema:
          type: integer
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResult'
  /inbound/{provider}:
    post:
      tags:
      - Inbound
      summary: Receive inbound webhook from a provider
      description: 'Accepts webhooks from external providers (Stripe, GitHub, etc.)

        and routes them to the customer''s endpoints.

        '
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
          description: Provider name (e.g. "stripe", "github")
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Webhook accepted
        '400':
          description: Invalid payload
  /inbound/{provider}/{endpoint_id}:
    post:
      tags:
      - Inbound
      summary: Receive inbound webhook for a specific endpoint
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
      - name: endpoint_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Webhook accepted
  /notifications:
    get:
      tags:
      - Notifications
      summary: List notifications
      parameters:
      - name: page
        in: query
        schema:
          type: integer
      - name: per_page
        in: query
        schema:
          type: integer
      responses:
        '200':
          description: Notification list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationListResponse'
  /notifications/unread-count:
    get:
      tags:
      - Notifications
      summary: Get unread notification count
      responses:
        '200':
          description: Unread count
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
  /notifications/read-all:
    put:
      tags:
      - Notifications
      summary: Mark all notifications as read
      responses:
        '200':
          description: All marked as read
  /notifications/{id}/read:
    put:
      tags:
      - Notifications
      summary: Mark notification as read
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Marked as read
  /notifications/{id}:
    delete:
      tags:
      - Notifications
      summary: Delete notification
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Notification deleted
  /devices:
    get:
      tags:
      - Devices
      summary: List registered devices
      responses:
        '200':
          description: Device list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DeviceTokenResponse'
    post:
      tags:
      - Devices
      summary: Register device for push notifications
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterDeviceRequest'
      responses:
        '201':
          description: Device registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceTokenResponse'
  /devices/{token}:
    delete:
      tags:
      - Devices
      summary: Remove device token
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Device removed
  /teams:
    get:
      tags:
      - Teams
      summary: List teams
      responses:
        '200':
          description: Team list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Team'
    post:
      tags:
      - Teams
      summary: Create a team
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTeamRequest'
      responses:
        '201':
          description: Team created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Team'
  /teams/{id}:
    get:
      tags:
      - Teams
      summary: Get team details
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Team details with members and invites
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamDetailResponse'
  /teams/{id}/invite:
    post:
      tags:
      - Teams
      summary: Invite a member to the team
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InviteRequest'
      responses:
        '200':
          description: Invitation sent
  /teams/{id}/members:
    get:
      tags:
      - Teams
      summary: List team members
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Member list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TeamMember'
  /teams/{id}/members/{uid}:
    delete:
      tags:
      - Teams
      summary: Remove member from team
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: uid
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Member removed
  /teams/{id}/members/{uid}/role:
    put:
      tags:
      - Teams
      summary: Change member role
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: uid
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeRoleRequest'
      responses:
        '200':
          description: Role changed
  /billing/subscription:
    get:
      tags:
      - Billing
      summary: Get current subscription
      responses:
        '200':
          description: Subscription details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponse'
  /billing/upgrade:
    post:
      tags:
      - Billing
      summary: Upgrade plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpgradeRequest'
      responses:
        '200':
          description: Checkout URL or direct upgrade
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpgradeResponse'
  /billing/portal:
    post:
      tags:
      - Billing
      summary: Open customer billing portal
      responses:
        '200':
          description: Portal URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
  /billing/usage:
    get:
      tags:
      - Billing
      summary: Get current usage
      responses:
        '200':
          description: Usage data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageResponse'
  /billing/invoices:
    get:
      tags:
      - Billing
      summary: List invoices
      responses:
        '200':
          description: Invoice list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/InvoiceResponse'
  /billing/webhook:
    post:
      tags:
      - Billing
      summary: Stripe webhook receiver
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Webhook processed
  /billing/webhook/polar:
    post:
      tags:
      - Billing
      summary: Polar.sh webhook receiver
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Webhook processed
  /billing/webhook/iyzico:
    post:
      tags:
      - Billing
      summary: iyzico webhook receiver
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Webhook processed
  /templates:
    get:
      tags:
      - Templates
      summary: List available templates
      parameters:
      - name: category
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Template list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookTemplate'
  /templates/{id}:
    get:
      tags:
      - Templates
      summary: Get template by ID
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Template details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookTemplate'
  /templates/{id}/apply:
    post:
      tags:
      - Templates
      summary: Apply template to an endpoint
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApplyTemplateRequest'
      responses:
        '200':
          description: Template applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplyTemplateResponse'
  /schemas:
    get:
      tags:
      - Schemas
      summary: List registered schemas
      responses:
        '200':
          description: Schema list
    post:
      tags:
      - Schemas
      summary: Register a new JSON Schema
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterSchemaRequest'
      responses:
        '201':
          description: Schema registered
  /schemas/{id}:
    get:
      tags:
      - Schemas
      summary: Get schema by ID
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Schema details
  /schemas/{id}/validate:
    post:
      tags:
      - Schemas
      summary: Validate an event against a schema
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ValidateEventRequest'
      responses:
        '200':
          description: Validation result
        '422':
          description: Validation failed
  /endpoints/{id}/routing:
    get:
      tags:
      - Routing
      summary: Get routing config for endpoint
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Routing info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoutingInfo'
    put:
      tags:
      - Routing
      summary: Update routing config
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRoutingRequest'
      responses:
        '200':
          description: Routing updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoutingInfo'
  /endpoints/{id}/health:
    get:
      tags:
      - Routing
      summary: Get endpoint health status
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Health status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EndpointHealth'
  /playground:
    get:
      tags:
      - Playground
      summary: Get playground info (endpoints, sample payloads)
      responses:
        '200':
          description: Playground data
          content:
            application/json:
              schema:
                type: object
                properties:
                  endpoints:
                    type: array
                    items:
                      type: object
                  sample_payloads:
                    type: array
                    items:
                      type: object
  /playground/test:
    post:
      tags:
      - Playground
      summary: Test a webhook delivery
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestWebhookRequest'
      responses:
        '200':
          description: Test result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestWebhookResponse'
  /endpoints/{endpoint_id}/transforms:
    get:
      tags:
      - Transforms
      summary: List transform rules for endpoint
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Transform rule list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TransformRule'
    post:
      tags:
      - Transforms
      summary: Create transform rule
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTransformRuleRequest'
      responses:
        '201':
          description: Rule created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransformRule'
  /endpoints/{endpoint_id}/transforms/{id}:
    put:
      tags:
      - Transforms
      summary: Update transform rule
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Rule updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransformRule'
    delete:
      tags:
      - Transforms
      summary: Delete transform rule
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Rule deleted
  /endpoints/{endpoint_id}/transforms/test:
    post:
      tags:
      - Transforms
      summary: Test a transform rule
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
                rule_id:
                  $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Transformed output
  /stream/deliveries:
    get:
      tags:
      - Stream
      summary: Real-time delivery event stream (SSE)
      description: Server-Sent Events stream of webhook deliveries
      parameters:
      - name: endpoint_id
        in: query
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: status
        in: query
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          default: 50
      responses:
        '200':
          description: SSE stream
          content:
            text/event-stream:
              schema:
                type: string
  /contact:
    post:
      tags:
      - Contact
      summary: Send contact form message
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactRequest'
      responses:
        '200':
          description: Message sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactResponse'
        '400':
          description: Validation error
  /outbound-ips:
    get:
      tags:
      - Outbound IPs
      summary: Get outbound IP addresses for firewall whitelisting
      security: []
      responses:
        '200':
          description: IP list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboundIpsResponse'
  /status:
    get:
      tags:
      - Health
      summary: System status (public)
      security: []
      responses:
        '200':
          description: System operational
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemStatus'
        '503':
          description: System degraded or down
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemStatus'
  /portal/me:
    get:
      tags:
      - Customer Portal
      summary: Get portal profile
      responses:
        '200':
          description: Profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalProfile'
    put:
      tags:
      - Customer Portal
      summary: Update portal profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileRequest'
      responses:
        '200':
          description: Profile updated
  /portal/api-keys:
    get:
      tags:
      - Customer Portal
      summary: List API keys (portal)
      responses:
        '200':
          description: API key list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ApiKeyInfo'
    post:
      tags:
      - Customer Portal
      summary: Create API key (portal)
      responses:
        '201':
          description: API key created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateApiKeyResponse'
  /portal/api-keys/{key_id}:
    delete:
      tags:
      - Customer Portal
      summary: Revoke API key (portal)
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Key revoked
  /portal/usage:
    get:
      tags:
      - Customer Portal
      summary: Get usage (portal)
      responses:
        '200':
          description: Usage data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageResponse'
  /portal/plan:
    get:
      tags:
      - Customer Portal
      summary: Get plan info (portal)
      responses:
        '200':
          description: Plan details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponse'
  /portal/notifications:
    get:
      tags:
      - Customer Portal
      summary: Get notification preferences (portal)
      responses:
        '200':
          description: Notification preferences
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreferences'
    put:
      tags:
      - Customer Portal
      summary: Update notification preferences (portal)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateNotificationPreferences'
      responses:
        '200':
          description: Preferences updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  updated:
                    type: boolean
                  preferences:
                    $ref: '#/components/schemas/NotificationPreferences'
  /endpoint-health:
    get:
      tags:
      - Health
      summary: List endpoint health statuses
      responses:
        '200':
          description: Health list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/EndpointHealth'
  /endpoint-health/{id}:
    get:
      tags:
      - Health
      summary: Get specific endpoint health
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Health status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EndpointHealth'
  /admin/users:
    get:
      tags:
      - Admin
      summary: List all users (admin)
      description: Returns paginated list of users with optional filters
      security:
      - BearerAuth: []
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 20
          maximum: 200
      - name: search
        in: query
        schema:
          type: string
        description: Search by email or name (ILIKE)
      - name: plan
        in: query
        schema:
          type: string
          enum: [free, pro, business]
        description: Filter by plan
      - name: status
        in: query
        schema:
          type: string
          enum: [active, banned]
        description: Filter by status
      - name: created_after
        in: query
        schema:
          type: string
          format: date
        description: Filter users created after this date (ISO 8601)
      - name: created_before
        in: query
        schema:
          type: string
          format: date
        description: Filter users created before this date (ISO 8601)
      responses:
        '200':
          description: Paginated user list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUsers'
  /admin/users/{id}:
    get:
      tags:
      - Admin
      summary: Get user details (admin)
      description: Returns user details with endpoints, recent deliveries, and usage stats
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: User details
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/UserSummary'
                  endpoints:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          $ref: '#/components/schemas/Uuid'
                        url:
                          type: string
                        description:
                          type: string
                          nullable: true
                        is_active:
                          type: boolean
                        created_at:
                          $ref: '#/components/schemas/DateTime'
                  recent_deliveries:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          $ref: '#/components/schemas/Uuid'
                        endpoint_id:
                          $ref: '#/components/schemas/Uuid'
                        status:
                          type: string
                        event_type:
                          type: string
                          nullable: true
                        created_at:
                          $ref: '#/components/schemas/DateTime'
                        attempt_count:
                          type: integer
                  usage_stats:
                    type: object
                    properties:
                      total_deliveries:
                        type: integer
                      success_rate:
                        type: number
                        format: double
                      endpoints_count:
                        type: integer
  /admin/users/{id}/plan:
    put:
      tags:
      - Admin
      summary: Change user plan (admin)
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                plan:
                  type: string
                  enum:
                  - free
                  - pro
                  - business
      responses:
        '200':
          description: Plan changed
  /admin/users/{id}/status:
    put:
      tags:
      - Admin
      summary: Change user status (admin)
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
      responses:
        '200':
          description: Status changed
  /admin/stats:
    get:
      tags:
      - Admin
      summary: System-wide statistics (admin)
      security:
      - BearerAuth: []
      responses:
        '200':
          description: System stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemStats'
  /admin/revenue:
    get:
      tags:
      - Admin
      summary: Revenue analytics (admin)
      description: Returns monthly revenue, revenue by plan, MRR, churn rate, and MRR trend
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Revenue data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RevenueResponse'
  /auth/logout:
    post:
      tags:
      - Auth
      summary: Logout (invalidate refresh token)
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Logged out
  /auth/export:
    get:
      tags:
      - Auth
      summary: Export user data (GDPR)
      security:
      - BearerAuth: []
      responses:
        '200':
          description: User data export
  /auth/account:
    delete:
      tags:
      - Auth
      summary: Delete account (GDPR)
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Account deleted
  /oauth/providers:
    get:
      tags:
      - OAuth
      summary: List available OAuth providers
      responses:
        '200':
          description: Provider list
  /oauth/google:
    get:
      tags:
      - OAuth
      summary: Google OAuth login redirect
      responses:
        '302':
          description: Redirect to Google
  /oauth/google/callback:
    get:
      tags:
      - OAuth
      summary: Google OAuth callback
      responses:
        '200':
          description: Authenticated
  /audit-log:
    get:
      tags:
      - Audit Log
      summary: List audit log entries
      security:
      - BearerAuth: []
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: action
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Audit entries
  /audit-log/{id}:
    get:
      tags:
      - Audit Log
      summary: Get audit entry detail
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Audit entry detail
  /sso/config:
    get:
      tags:
      - SSO
      summary: Get SSO configuration
      security:
      - BearerAuth: []
      responses:
        '200':
          description: SSO config
    post:
      tags:
      - SSO
      summary: Create/update SSO configuration
      security:
      - BearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                provider:
                  type: string
                  enum:
                  - saml
                  - oidc
                enabled:
                  type: boolean
      responses:
        '200':
          description: SSO config saved
    delete:
      tags:
      - SSO
      summary: Delete SSO configuration
      security:
      - BearerAuth: []
      responses:
        '200':
          description: SSO config deleted
  /sso/test:
    post:
      tags:
      - SSO
      summary: Test SSO connection
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Connection test result
  /custom-domains:
    get:
      tags:
      - Custom Domains
      summary: List custom domains
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Domain list
    post:
      tags:
      - Custom Domains
      summary: Add custom domain
      security:
      - BearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
      responses:
        '201':
          description: Domain added
  /custom-domains/{id}:
    delete:
      tags:
      - Custom Domains
      summary: Delete custom domain
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Domain deleted
  /custom-domains/{id}/verify:
    post:
      tags:
      - Custom Domains
      summary: Verify domain ownership
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Verification result
  /rate-limits:
    get:
      tags:
      - Rate Limits
      summary: List rate limits
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Rate limit list
  /rate-limits/{endpoint_id}:
    get:
      tags:
      - Rate Limits
      summary: Get rate limit for endpoint
      security:
      - BearerAuth: []
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Rate limit config
    post:
      tags:
      - Rate Limits
      summary: Set rate limit for endpoint
      security:
      - BearerAuth: []
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Rate limit set
    delete:
      tags:
      - Rate Limits
      summary: Delete rate limit for endpoint
      security:
      - BearerAuth: []
      parameters:
      - name: endpoint_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Rate limit deleted
  /embed:
    get:
      tags:
      - Embed
      summary: Embeddable portal HTML
      responses:
        '200':
          description: HTML page
  /embed/script:
    get:
      tags:
      - Embed
      summary: Embeddable portal JavaScript
      responses:
        '200':
          description: JavaScript file
  /events:
    get:
      tags:
      - Events
      summary: List event types
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Event type list
  /simulator:
    post:
      tags:
      - Simulator
      summary: Simulate a webhook delivery
      security:
      - BearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                endpoint_id:
                  type: string
                event:
                  type: string
                data:
                  type: object
      responses:
        '200':
          description: Simulation result
  /portal/config:
    get:
      tags:
      - Customer Portal
      summary: Get portal configuration
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Portal config
    post:
      tags:
      - Customer Portal
      summary: Update portal configuration
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Portal config saved
  /portal/embed-code:
    get:
      tags:
      - Customer Portal
      summary: Get portal embed code
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Embed code snippet
  /admin/sdk-update:
    post:
      tags:
      - Admin
      summary: Send SDK update notification to users
      security:
      - BearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                version:
                  type: string
                message:
                  type: string
      responses:
        '200':
          description: Notification sent
  /admin/alerts:
    get:
      tags:
      - Admin
      summary: List all alert rules (admin)
      description: Returns all alert rules for the authenticated admin's account
      security:
      - BearerAuth: []
      responses:
        '200':
          description: List of alert rules
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminAlertRule'
    post:
      tags:
      - Admin
      summary: Create a platform alert rule (admin)
      security:
      - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminCreateAlertRequest'
      responses:
        '201':
          description: Alert rule created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAlertRule'
  /admin/alerts/{id}:
    put:
      tags:
      - Admin
      summary: Update an alert rule (admin)
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminUpdateAlertRequest'
      responses:
        '200':
          description: Alert rule updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAlertRule'
    delete:
      tags:
      - Admin
      summary: Delete an alert rule (admin)
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Alert rule deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
  /admin/audit-logs:
    get:
      tags:
      - Admin
      summary: List audit logs (admin)
      description: Returns all audit log entries across all users
      security:
      - BearerAuth: []
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
      - name: action
        in: query
        schema:
          type: string
          description: Filter by action type (e.g. IMPERSONATE, LOGIN)
      - name: admin_id
        in: query
        schema:
          $ref: '#/components/schemas/Uuid'
          description: Filter by admin user ID
      responses:
        '200':
          description: Paginated audit log entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAuditLogResponse'
  /admin/churn:
    get:
      tags:
      - Admin
      summary: Get churn metrics (admin)
      description: Lists users who became inactive in the last 30 days
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Churn report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChurnResponse'
  /admin/deliveries/{id}/replay:
    post:
      tags:
      - Admin
      summary: Replay a delivery (admin)
      description: Creates a new delivery with the same payload as the original
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
        description: Original delivery ID to replay
      responses:
        '200':
          description: Delivery replayed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReplayDeliveryResponse'
  /admin/revenue/export:
    get:
      tags:
      - Admin
      summary: Export revenue data as CSV (admin)
      security:
      - BearerAuth: []
      parameters:
      - name: format
        in: query
        schema:
          type: string
          default: csv
          enum: [csv]
      - name: months
        in: query
        schema:
          type: integer
          default: 12
          minimum: 1
          maximum: 60
        description: Number of months to include
      responses:
        '200':
          description: Revenue CSV export
          content:
            text/csv:
              schema:
                type: string
  /admin/settings:
    get:
      tags:
      - Admin
      summary: Get platform settings (admin)
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Platform settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformSettings'
    put:
      tags:
      - Admin
      summary: Update platform settings (admin)
      security:
      - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlatformSettings'
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /admin/test-webhook:
    post:
      tags:
      - Admin
      summary: Send a test webhook to a URL (admin)
      description: Sends an HTTP POST to the specified URL with SSRF protection
      security:
      - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminTestWebhookRequest'
      responses:
        '200':
          description: Test webhook result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminTestWebhookResponse'
  /admin/users/export:
    get:
      tags:
      - Admin
      summary: Export users as CSV (admin)
      security:
      - BearerAuth: []
      parameters:
      - name: format
        in: query
        schema:
          type: string
          default: csv
          enum: [csv]
      - name: plan
        in: query
        schema:
          type: string
          enum: [free, pro, business]
        description: Filter by plan
      - name: status
        in: query
        schema:
          type: string
          enum: [active, banned]
        description: Filter by status
      responses:
        '200':
          description: Users CSV export
          content:
            text/csv:
              schema:
                type: string
  /admin/users/{id}/analytics:
    get:
      tags:
      - Admin
      summary: Get user analytics (admin)
      description: Returns delivery analytics for a specific user over a time period
      security:
      - BearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: days
        in: query
        schema:
          type: integer
          default: 30
          minimum: 1
          maximum: 365
        description: Number of days to analyze
      responses:
        '200':
          description: User analytics data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAnalytics'
  /oauth/github:
    get:
      tags:
      - OAuth
      summary: GitHub OAuth login redirect
      description: Redirects to GitHub OAuth consent screen with CSRF state cookie
      responses:
        '302':
          description: Redirect to GitHub OAuth
  /oauth/github/callback:
    get:
      tags:
      - OAuth
      summary: GitHub OAuth callback
      description: Handles GitHub OAuth callback, creates/links account, sets auth cookies
      parameters:
      - name: code
        in: query
        required: true
        schema:
          type: string
        description: Authorization code from GitHub
      - name: state
        in: query
        required: true
        schema:
          type: string
        description: CSRF state token (verified against cookie)
      - name: error
        in: query
        schema:
          type: string
        description: Error from GitHub (e.g. access_denied)
      responses:
        '302':
          description: Redirect to app with auth cookies set
        '400':
          description: Invalid or missing authorization code
