steps:
  # Run database migrations
  - name: 'node:22-slim'
    id: 'run-migrations'
    entrypoint: 'sh'
    args:
      - '-c'
      - 'npm install pg && node run-migrations.js'
    secretEnv: ['DATABASE_URL']

  # Build API image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '--no-cache', '-f', 'Dockerfile.api', '-t', 'europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/api:latest', '.']
    id: 'build-api'

  # Build Worker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '--no-cache', '-f', 'Dockerfile.worker', '-t', 'europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/worker:latest', '.']
    id: 'build-worker'

  # Push both images
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/api:latest']
    id: 'push-api'
    waitFor: ['build-api']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/worker:latest']
    id: 'push-worker'
    waitFor: ['build-worker']

  # Deploy API to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'hooksniff-api'
      - '--image=europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/api:latest'
      - '--region=europe-west1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=3000'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--min-instances=1'
      - '--max-instances=3'
      - '--timeout=3600'
      - '--set-env-vars=APP_ENV=production,RUST_LOG=info,RATE_LIMIT_STORE=redis,LOG_FORMAT=json,WEBHOOK_FORMAT=standard,MAX_PAYLOAD_BYTES=1048576,RETENTION_DAYS=30,WEBHOOK_TIMESTAMP_TOLERANCE_SECS=300,APP_URL=https://hooksniff.vercel.app,CORS_ORIGINS=https://hooksniff.vercel.app,OTEL_ENABLED=true,OTEL_EXPORTER_OTLP_ENDPOINT=https://o4511400113471488.ingest.de.sentry.io/api/4511423659704400/integration/otlp,NOTIFY_EMAIL=servetarslan02@gmail.com,EVENT_PUBLISHER_ENABLED=true,WS_ENABLED=true,WS_MAX_CONNECTIONS=100,WS_MAX_CONNECTIONS_PER_USER=5,WS_HEARTBEAT_INTERVAL_SECS=30,WS_SHUTDOWN_TIMEOUT_SECS=10,OAUTH_REDIRECT_BASE=https://hooksniff-api-1046140057667.europe-west1.run.app'
      - '--set-secrets=DATABASE_URL=neon-db-url:latest,REDIS_URL=upstash-redis-url:latest,JWT_SECRET=jwt-secret:latest,HMAC_SECRET=hmac-secret:latest,POLAR_ACCESS_TOKEN=polar-token:latest,POLAR_WEBHOOK_SECRET=polar-webhook:latest,POLAR_PRODUCT_PRO=polar-pro:latest,POLAR_PRODUCT_BUSINESS=polar-business:latest,OTEL_EXPORTER_OTLP_HEADERS=otel-headers:latest,GCP_SA_JSON=gcp-sa-json:latest,RESEND_API_KEY=resend-api-key:latest,ENCRYPTION_KEY=encryption-key:latest,POLAR_STARTUP_TRIAL_DISCOUNT_ID=polar-startup-trial-discount-id:latest,GOOGLE_CLIENT_ID=google-client-id:latest,GOOGLE_CLIENT_SECRET=google-client-secret:latest,GITHUB_CLIENT_ID=github-client-id:latest,GITHUB_CLIENT_SECRET=github-client-secret:latest'
      - '--project=hooksniff-app'
    waitFor: ['push-api', 'run-migrations']

  # Deploy Worker to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'hooksniff-worker'
      - '--image=europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/worker:latest'
      - '--region=europe-west1'
      - '--platform=managed'
      - '--no-allow-unauthenticated'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--min-instances=1'
      - '--max-instances=4'
      - '--set-env-vars=APP_ENV=production,RUST_LOG=info,RATE_LIMIT_STORE=redis,LOG_FORMAT=json,OTEL_ENABLED=true,OTEL_EXPORTER_OTLP_ENDPOINT=https://o4511400113471488.ingest.de.sentry.io/api/4511423659704400/integration/otlp'
      - '--set-secrets=DATABASE_URL=neon-db-url:latest,REDIS_URL=upstash-redis-url:latest,JWT_SECRET=jwt-secret:latest,HMAC_SECRET=hmac-secret:latest,GCP_SA_JSON=gcp-sa-json:latest,ENCRYPTION_KEY=encryption-key:latest'
      - '--project=hooksniff-app'
    waitFor: ['push-worker']

images:
  - 'europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/api:latest'
  - 'europe-west1-docker.pkg.dev/hooksniff-app/hooksniff/worker:latest'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

availableSecrets:
  secretManager:
    - versionName: projects/hooksniff-app/secrets/neon-db-url/versions/latest
      env: DATABASE_URL
